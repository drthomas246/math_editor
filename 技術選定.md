# 数学プリント作成ソフト 技術選定書 v1.1

## 1. 文書の目的

本書は、中学校数学向けプリント作成SPAのMVP実装に使用する技術と、各技術の責務境界を定義する。v1.1では次の2点を明文化する。

- 永続データ形式の正本をZod Schemaへ一本化する。
- 画像BlobをWorksheetから分離し、`assetId`参照へ統一する。

## 2. 採用技術

| 領域 | 採用技術 | 主な責務 |
|---|---|---|
| UI | React + TypeScript | 編集画面、一覧画面、ダイアログ |
| ビルド | Vite | 開発サーバー、バンドル |
| WYSIWYG | Tiptap | 構造化リッチテキスト編集 |
| 数式入力 | MathLive | 見たままの数式編集、LaTeX保持 |
| PDF生成 | React PDF | Worksheet ASTからPDFを生成 |
| 高速プレビュー | HTML / CSS Preview Renderer | 編集内容の差分表示、ページ組版、ズーム |
| UIフォント | Inter、Funnel Sans、Geist、Geist Mono＋固定BIZ UDPゴシック | 公式最新版を実装開始時に取得し、アプリへ固定してセルフホスト |
| 印刷用日本語フォント | BIZ UDPゴシック、BIZ UDゴシック、BIZ UDP明朝、Noto Sans JP、Noto Serif JP | HTML表示とPDF埋込み。初期値はBIZ UDPゴシック |
| UI状態 | Zustand | 選択状態、画面状態、プレビュー状態 |
| ローカルDB | IndexedDB + Dexie | Worksheetと画像Blobの自動保存 |
| 検証・型 | Zod 4 | 保存形式の正本、実行時検証、型導出 |
| 並べ替え | dnd-kit | 問題・コンテンツ・小問のドラッグ操作 |
| Undo / Redo | Immer patches | Worksheet全体の変更履歴 |
| 単体テスト | Vitest | Domain、Schema、Repositoryのテスト |
| UIテスト | Testing Library | 編集操作と状態連携のテスト |
| E2E | Playwright | 作成・保存・復元・PDF出力の通しテスト |

## 3. 永続データの正本

`src/domain/worksheet/worksheet.schema.ts`に定義するZod Schemaを、保存形式に関する唯一の正本とする。

```text
worksheet.schema.ts（Zod）
        │
        ├─ z.infer
        │    └─ worksheet.ts（TypeScript型）
        │
        └─ z.toJSONSchema()
             └─ schemas/math-worksheet.schema.json
```

運用規則は次のとおり。

- `worksheet.ts`へ同じ構造の`interface`を手書きしない。
- JSON Schemaを直接編集しない。
- スキーマ変更後は`npm run schema:generate`を実行する。
- CIで`npm run schema:check`を実行し、生成物の差分を検出する。
- 外部ファイル読込とIndexedDB読込の境界でZod検証する。

## 4. 画像データの責務分離

### 4.1 Worksheet側

`ImageBlock`はレイアウトと参照情報だけを保持する。

```ts
{
  id: EntityId;
  type: "image";
  assetId: EntityId;
  alt: string;
  placement: "block" | "floatLeft" | "floatRight";
  widthPercent: 25 | 33 | 50 | 66 | 75 | 100;
}
```

`placement`が`floatLeft`または`floatRight`の場合、`widthPercent`は25、33、50だけを許可する。`block`では6種類すべてを許可する。この条件はUIとZod Schemaの両方で検証する。

`dataUrl`と`mimeType`は`ImageBlock`へ保存しない。

### 4.2 IndexedDB側

画像本体はDexieの`assets`テーブルへ`Blob`として分離保存する。

```text
worksheets
  key: id
  value: Worksheet

assets
  key: id
  index: worksheetId
  value: AssetRecord
```

`AssetRecord`は`id`、`worksheetId`、`mimeType`、`blob`、画像寸法、作成日時を保持する。ゴミ箱への移動ではAssetを保持し、Worksheetの完全削除時は同じ`worksheetId`を持つassetsも同一トランザクションで削除する。

### 4.3 表示時

Repository層が`assetId`からBlobを取得し、表示専用のObject URLを生成する。Object URLはUIの一時データであり、Worksheet・Zustandの永続対象・Undo履歴へ保存しない。不要になったObject URLは`URL.revokeObjectURL()`で解放する。

### 4.4 バックアップ時

バックアップファイルは単一・全体で同じ`format: "math-worksheet"`を使用し、`kind`で判別する。

```text
MathWorksheetSingleFile
├─ format: "math-worksheet"
├─ kind: "single"
├─ version: 1
├─ exportedAt
├─ worksheet         画像本体を含まない
└─ assets[]          BlobをBase64化したBackupAsset

MathWorksheetArchive
├─ format: "math-worksheet"
├─ kind: "archive"
├─ version: 1
├─ exportedAt
├─ worksheets[1..2000]  deletedAt === nullの通常一覧だけ
└─ assets[]             対象Worksheetから参照されるBackupAssetだけ
```

エクスポート時だけBlobをBase64へ変換し、`dataBase64`へ格納する。data URLプレフィックスは付けず、MIME型は`mimeType`へ分離する。全体バックアップはゴミ箱内のWorksheetとその専用Assetを含めない。インポート時はBase64をBlobへ戻し、格納されたWorksheet ID、各`asset.worksheetId`、`ImageBlock.assetId`、RichText内`imageRef.assetId`の参照整合性を検証してからDexieへ保存する。単一・全体とも余剰Assetを拒否し、全体バックアップは1件でも不正なら全件をrollbackする。

## 5. Schemaの役割分担

| Schema | 対象 | JSON Schema生成対象 |
|---|---|---|
| `WorksheetSchema` | IndexedDBのworksheetsレコード | 間接的に含む |
| `AssetRecordSchema` | IndexedDBのassetsレコード（Blob） | 対象外 |
| `BackupAssetSchema` | JSONバックアップ内のBase64画像 | 対象 |
| `MathWorksheetSingleFileSchema` | 単一プリントの再編集用バックアップ | ルートの分岐 |
| `MathWorksheetArchiveSchema` | 通常一覧全体の再編集用バックアップ | ルートの分岐 |
| `MathWorksheetFileSchema` | `kind`で単一・全体を判別するバックアップ共通ルート | ルート |

BlobはJSON Schemaで表現せず、JSON形式では`BackupAssetSchema`だけを使用する。

## 6. レイヤー構成

```text
src/
├─ domain/
│  └─ worksheet/
│     ├─ worksheet.schema.ts
│     ├─ worksheet.ts
│     ├─ worksheet.commands.ts
│     └─ worksheet.input-version.ts
├─ application/
│  ├─ repositories/
│  ├─ backup/
│  └─ pdf/
├─ infrastructure/
│  ├─ indexeddb/
│  ├─ asset-url/
│  └─ file/
└─ presentation/
   ├─ editor/
   ├─ preview/
   └─ worksheet-list/
```

- Presentation層からWorksheetを直接変更せず、Domain Commandを通す。
- Repository境界でZod検証を行う。
- PDF生成はObject URLやBlobレコードではなく、解決済み画像データを受け取るアプリケーションサービス経由とする。
- 問題本文、囲み枠、小問本文、教師用正解・解説では、画像と表をRichText内のImageRefNode、RichTableNodeとして扱う。表セルではImageRefNodeだけを追加で許可し、RichTableNodeを禁止する。問題直下の独立ImageBlock、TableBlockも併用する。
- ImageRefNodeの幅は所属コンテナの内側幅を基準とし、表セル内画像は親表の測定・次ページ移動・全体縮小へ含める。
- UI用・印刷用フォントは実装開始時点の公式最新版を採用後、manifestにreleaseまたはcommit、ファイル名、SHA-256、ライセンスを記録して固定し、外部CDN、OSフォント、自動更新へ依存しない。

## 7. 自動保存

- Worksheet変更は最後の変更から750msのデバウンス後に`worksheets`へ保存する。
- 新規画像追加は、AssetRecord作成とImageBlock追加を一つのユースケースとして扱う。
- 画像削除は参照状況を確認し、未参照になったAssetRecordを削除する。
- 保存状態は「保存中」「保存済み」「保存失敗」をUIへ表示する。
- 書込み失敗時に編集データを破棄しない。

## 8. Undo / Redo

Undo / RedoはImmer patchesでWorksheet全体を対象とする。ただしBlobとObject URLは履歴へ含めない。画像操作のUndo / Redoでは`assetId`の参照変更を履歴化し、Blobの物理削除は履歴から参照されなくなったことを確認して遅延実行する。

## 9. PDFとプレビュー

- React PDFで問題のみ、解答付き、問題＋解答を同じWorksheetから生成する。問題を省略した解答のみのモードは設けない。問題＋解答はPDF出力専用とする。
- 右ペインは問題のみ、解答付きの2モードを共有RenderModelからHTML / CSSページとして差分描画し、ズーム機能を提供する。
- 文字入力は編集領域へ即時反映し、右ペインは最後の変更から50msのdebounce後に差分更新を開始する。
- 数値ズームは25～200%を25%刻みとし、初期値を100%にする。ズーム変更では再組版しない。
- React PDFとPDF Workerは「PDFをダウンロード」の実行時だけ起動し、編集プレビューの更新経路から分離する。
- 画像はPDF生成前に`assetId`を解決する。
- 小問とその解答欄は原則として同じページへ配置する。
- 1問がページを超える場合だけ、定義した分割可能位置で分割する。
- HTMLプレビューとPDFは各文章・数式ブロックで概ね1行以内の折り返し差を許容するが、問題開始ページ、明示改ページ、内容順序は一致させ、欠落・重なり・はみ出しを許容しない。

## 10. セキュリティ

- 画像は1点10MiB以下、同一Worksheet内の画像合計50MiB以下、幅・高さ各10,000px以下、総画素数40MP以下に制限し、入力時とバックアップ復元時に検証する。
- SVGはMVPでは受け付けず、ファイル拡張子や申告MIME型にかかわらず入力時に拒否する。
- MIME型はファイル名だけで判断しない。
- Base64インポート後の実バイト数にも上限を適用する。
- Tiptap JSONは許可したノード・属性だけをSchemaで検証する。

## 11. テスト方針

最低限、次を自動テストする。

- `ImageBlock`が`assetId`必須で、`dataUrl`・`mimeType`を拒否する。
- `AssetRecord`がBlobを受け付ける。
- `MathWorksheetFile`が不足アセット、重複ID、不一致`worksheetId`を拒否する。
- 表が1～20行×1～20列の範囲だけを受け付け、作成、行列追加、貼り付け、JSON読込みで上限が一致する。
- 解答欄とスペーサーが1～20行の範囲だけを受け付け、UI操作とJSON読込みで上限が一致する。
- Zodから再生成したJSON Schemaとコミット済みファイルが一致する。
- 旧`dataUrl`形式とversion 1以外の入力を保存前に拒否できる。
- ゴミ箱移動では関連assetsを保持し、Worksheetの完全削除時に関連assetsが残らない。
- バックアップのexport/importで画像バイト列と寸法が維持される。
- 全体バックアップの1件が不正な場合に全件をrollbackし、端末内データを変更しない。
- PDFの問題のみ・解答付き・問題＋解答の3モードで画像が正しく解決される。

## 12. 入力バージョンと旧形式

本アプリは未公開で移行対象となる既存データがないため、MVPでは旧形式のマイグレーションを実装しない。`ImageBlock.dataUrl`、ImageBlock内の`mimeType`、またはversion 1以外を検出した場合は、データを変換・保存せずエラーとして拒否する。

公開後に保存形式を変更する必要が生じた場合だけ、旧versionごとの純粋なマイグレーション関数と往復テストを追加する。

## 13. 実装開始条件

- 要件定義書、画面設計書、技術選定書、詳細設計書、`design.pen`、Zod Schema、生成JSON SchemaのMVP仕様が相互に矛盾しない。
- 未確定のMVP動作がなく、ページ分割、画像・表、文字サイズ、バックアップ、フォント取得方針が確定している。
- `npm run verify`が成功する。
- schema version 1を実装中に互換目的で変更せず、保存形式をZod Schemaから導出する方針が確定している。

上記を満たした時点でReact画面、Repository、PDF生成の実装を開始できる。フォントファイルの取得・manifest作成とアプリ依存関係の追加は、確定済み方針に従う実装作業へ含める。

## 14. MVP実装後の技術確認条件

- サンプルWorksheetをIndexedDBへ保存・再読込できる。
- assetId経由で画像を編集画面とPDFの両方へ表示できる。
- 画像を含むバックアップをexport/importできる。
- 標準5ページの編集でHTMLプレビュー反映の95パーセンタイルを150ms以内とし、50msを超えるLong Taskを発生させない。
