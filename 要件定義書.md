# 数学プリント作成ソフト 要件定義書（現行実装準拠版）

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 数学プリント作成ソフト 要件定義書 |
| 文書版 | 1.1（現行実装準拠） |
| データスキーマ版 | 1 |
| 基準日 | 2026-08-12 |
| 対象 | 中学校数学 |
| 提供形態 | PC向けWebアプリケーション |
| 正本 | `src/domain/worksheet/worksheet.schema.ts`および現行UI実装 |

本書は将来構想ではなく、現行コードで利用できる機能、制約、受け入れ条件を定義する。未実装事項は「既知の制約」に明記する。

## 2. システム概要

### 2.1 目的

中学校数学の授業・学習で使用するプリントをブラウザ上で作成し、生徒用と教師用の表示を同じデータから生成する。編集結果を端末内へ保存し、JSONバックアップとPDFダウンロードを提供する。

### 2.2 基本方針

- 1プリントを`Worksheet`、1問を`ProblemBlock`として構造化する。
- 左ペインで編集し、右ペインでページプレビューを表示する。
- 通常データと画像BlobをIndexedDBへ保存する。
- JSON SchemaとTypeScript型はZodスキーマから導出する。
- PDFはプレビューDOMをページごとに画像化して生成する。
- クラウドやサーバーを利用せず、ブラウザ内で完結する。

## 3. 対象環境と対象外

### 3.1 対象環境

- 最新版のGoogle ChromeまたはMicrosoft Edge
- 横幅1024px以上のPC画面での編集
- IndexedDB、Blob、Object URL、BroadcastChannel、`createImageBitmap`を利用できるブラウザ
- ビルドターゲットはChrome 107、Edge 107以降

ブラウザの必須APIが不足する場合は、非対応画面を表示する。横幅1024px未満では編集領域を非表示にし、PCサイズの画面を案内する。

### 3.2 対象外

- スマートフォンでの編集
- ユーザー登録、ログイン、アカウント管理
- クラウド保存、同期、共有、共同編集
- フォルダ、タグ
- ページ番号の印字
- ページ全体の段組み
- 作図、グラフ作成
- 旧スキーマからの自動移行

## 4. 画面とルーティング

| パス | 画面 | 主な機能 |
|---|---|---|
| `/` | プリント一覧 | 新規作成、検索、複製、JSON入出力、ゴミ箱移動 |
| `/worksheets/:worksheetId` | 編集 | 問題編集、設定、プレビュー、PDF出力 |
| `/trash` | ゴミ箱 | 復元、完全削除、全件削除 |

定義外のパスは`/`へ置換遷移する。存在しない、またはゴミ箱内のプリントを編集URLで開いた場合は、見つからない旨を表示する。

## 5. プリント一覧

### 5.1 表示

- 通常一覧は`deletedAt === null`のプリントだけを対象とする。
- 更新日時の降順、同日時の場合はIDの昇順で表示する。
- 題名、用紙サイズ、更新日時、操作メニューを表示する。
- 1ページ50件とし、51件以上ではページングを表示する。
- 不正な保存データは削除せず、件数だけを警告表示する。

### 5.2 検索

- 検索対象は題名だけとする。
- 入力から150ms後に絞り込み、検索変更時は1ページ目へ戻す。
- Unicode NFC正規化、全角ASCIIと全角空白の半角化、英字の小文字化、前後空白の除去を行う。
- 内部空白は保持し、ひらがなとカタカナは同一視しない。

### 5.3 新規作成

「新しいプリント」を選択した時点で初期値のプリントを保存し、その編集画面へ遷移する。

初期値は以下とする。

| 項目 | 初期値 |
|---|---|
| 題名 | 無題のプリント |
| 用紙 | JIS B5、縦 |
| 余白 | 標準（15mm） |
| フォント設定値 | BIZ UDPゴシック |
| 問題番号 | `1.`形式 |
| ヘッダー | 年・組・番・名前をすべて表示 |
| 問題 | 本文1件を持つ問題を1件 |

### 5.4 プリント数上限

- IndexedDB内のプリントはゴミ箱を含め最大2,000件とする。
- 上限到達時は新規作成と複製を無効化する。
- インポート後に上限を超える場合も保存しない。
- 空きを作るにはプリントを完全削除する必要がある。

## 6. 編集画面

### 6.1 基本構成

- ヘッダー：一覧へ戻る、題名、保存状態、Undo / Redo、プリント設定、PDF出力
- 左ペイン：問題とコンテンツの編集
- 右ペイン：ページプレビュー
- ペイン比率：左35～65%、右65～35%

境界はポインター操作またはフォーカス時の左右矢印で変更できる。通常は2%刻み、Shift併用時は10%刻みとする。ペイン比率、プレビューモード、倍率はLocalStorageへ保存する。

### 6.2 題名

- 最大100文字とする。
- 入力中はその場で更新する。
- フォーカスを外した時点で空白だけの場合は「無題のプリント」へ補正する。
- 前後空白を除去し、ヘッダー題名と同期する。

### 6.3 保存状態

`保存済み`、`未保存`、`保存中…`、`保存できませんでした`を表示する。保存失敗時は再試行操作を提供する。

## 7. 問題ブロックと採番

### 7.1 構造上限

- 1プリントは1～200問を持つ。
- 最後の1問は削除できない。
- 1問は最大100個のトップレベルコンテンツを持つ。

### 7.2 問題操作

- 追加：選択中の問題の直後へ追加し、新しい問題を選択する。
- 複製：直後へ複製し、配下のエンティティIDを再生成する。
- 削除：残り1問の場合を除き削除できる。
- 並べ替え：問題メニューの「上へ移動」「下へ移動」を使用する。

ドラッグハンドルは表示されるが、ドラッグ＆ドロップ操作とAlt＋矢印キーによる移動は現行実装の要件に含めない。

### 7.3 問題番号

- 採番の有効／無効を問題ごとに設定できる。
- 採番しない問題は連番に数えない。
- 任意の問題から1以上の開始番号で振り直せる。
- 表示形式はプリント全体で`1`、`1.`、`1)`、`(1)`、`[1]`、`問1`から選ぶ。

## 8. コンテンツ

### 8.1 追加可能なトップレベルコンテンツ

「内容を追加」メニューから以下を追加できる。

- 本文
- 囲み枠
- 小問グループ
- 生徒用解答欄
- スペーサー
- 改ページ

選択したコンテンツは上下ボタンで1件ずつ移動し、削除できる。画像と表の独立ブロックは保存形式と表示に対応するが、現行UIの「内容を追加」には直接追加する項目がない。画像と表は本文、囲み枠、小問本文のエディタから挿入する。

### 8.2 本文エディタ

本文、囲み枠、小問本文では以下を使用できる。

- 文字サイズ：小、標準、大、特大
- 太字、下線、斜体
- 箇条書き、番号付きリスト
- 行内数式、独立数式
- 画像
- 表

段落は左・中央・右の揃え情報を保存できるが、現行UIに揃えを変更するボタンはない。外部HTMLの任意書式を保持することは保証せず、保存可能なノードとマークへ正規化する。

### 8.3 囲み枠

- 題名は空欄を許可する。
- 本文は通常の本文エディタを使用する。
- デザインはシンプル、見出し付き、帯見出し、強調の4種類とする。

### 8.4 解答欄とスペーサー

- 解答欄は横罫線または四角囲みを選ぶ。
- 解答欄の高さは1～20行とする。
- スペーサーの高さは1～20行とする。
- 小問の初期解答欄は横罫線1行、独立解答欄の初期値は横罫線2行とする。

### 8.5 改ページ

問題内部へ改ページコンテンツを挿入できる。プレビューとPDFでは、その直前までを現在ページへ置き、後続コンテンツを次ページから開始する。

## 9. 小問

- 小問グループは初期状態で2件の小問を持つ。
- 1グループは1～100件の小問を持つ。
- 小問番号は`(1)`形式で自動採番する。
- 任意の小問から1以上の開始番号で振り直せる。
- 小問は半幅または全幅を選択できる。
- 小問を追加・削除できるが、最後の1件は削除できない。
- 既存の小問解答欄は種類と高さを変更できる。

現行UIでは、小問の複製、並べ替え、番号形式変更、解答欄の有無切替、小問別の教師用正解編集を提供しない。保存形式は小問の`solution`を保持でき、インポートされた内容は解答モードで表示できる。

## 10. 数式

- MathLiveの数式フィールドで視覚的に入力する。
- 行内数式と独立数式を選択できる。
- 既存数式は数式上の編集ボタンからLaTeXと文字サイズを変更できる。
- 表セル内では行内数式だけを挿入する。
- 文字サイズは小、標準、大、特大とする。
- LaTeXは1～5,000文字とし、外部参照やHTML/CSS注入につながる禁止コマンドを拒否する。

数式パレットは次の12項目とする。

`+`、`-`、`×`、`÷`、`=`、`≠`、`±`、分数、平方根、2乗、`≤`、`≥`

最近使った数式、よく使う数式、図形記号の拡張パレットは提供しない。

## 11. 画像

### 11.1 挿入時検証

- 対応MIME型：PNG、JPEG、WebP
- 1点10MiB以下
- 幅、高さ：各10,000px以下
- 総画素数：40MP以下
- `createImageBitmap`でデコードできること

現行実装はブラウザが報告するMIME型を確認する。ファイルシグネチャの独自判定と、プリント内画像の合計容量制限は設けない。

### 11.2 表示設定

- 配置：独立、左回り込み、右回り込み
- 独立配置の幅：25%、33%、50%、66%、75%、100%
- 回り込みの幅：25%、33%、50%
- 代替テキストを保持する。
- 挿入後に画像差し替え、配置、幅、代替テキストを変更できる。

画像本体は`AssetRecord.blob`としてIndexedDBの`assets`テーブルへ保存し、文書から`assetId`で参照する。

## 12. 表

### 12.1 挿入テンプレート

| 種類 | 初期構造 |
|---|---|
| 一般 | 初期3行×4列。挿入前に1～20行、1～20列で変更可能 |
| 関数 | 2行×4列。先頭列に`x`、`y` |
| 度数分布 | 3行×2列。先頭行に`階級`、`度数` |

### 12.2 編集

- 最大20行×20列
- 選択セルの上／下へ行追加、行削除
- 選択セルの左／右へ列追加、列削除
- 右セルとの横結合、下セルとの縦結合、結合解除
- 行高は自動または5～100mm
- 列幅は1～99%で指定し、他列を再配分して合計100%を維持する。
- セル内では文字サイズ、太字、下線、斜体、行内数式を編集できる。

セル内の箇条書き、番号付きリスト、表の入れ子は許可しない。画像参照は保存形式で表現できるが、セルへ画像を新規挿入するUIは提供しない。

## 13. 教師用正解・解説

- 問題ごとに正解・解説ドキュメントを保持できる。
- 編集欄は文字サイズ、太字、下線、斜体、箇条書き、番号付きリストを提供する。
- 現行UIには、問題の正解・解説へ数式、画像、表を挿入するボタンがない。
- 空ドキュメントは解答表示対象とみなさない。
- 小問別の正解は保存・表示できるが、現行UIから編集できない。

## 14. プリント設定

### 14.1 用紙

- JIS B5：182×257mm
- A4：210×297mm
- 縦向き固定
- 余白：広い20mm、標準15mm、狭い10mm、かなり狭い5mm
- 5mm選択時は印刷欠けの注意を表示する。

### 14.2 フォント

設定値として以下を選択・保存できる。

- BIZ UDPゴシック
- BIZ UDゴシック
- BIZ UDP明朝
- Noto Sans JP
- Noto Serif JP

ただし現行CSSはプレビューとPDFで固定の`"BIZ UDPGothic", "Yu Gothic", sans-serif`スタックを使用する。選択値に応じた実フォント切替とWebフォント同梱は未実装とする。

### 14.3 ヘッダー

- 題名を中央表示する。
- 年、組、番、名前を項目ごとに表示／非表示にできる。
- 各出力セクションの先頭ページだけに表示する。
- 2ページ目以降へ簡易ヘッダーやページ番号を印字しない。

## 15. プレビューとページ分割

### 15.1 モード

- 問題のみ：問題と生徒用解答欄を表示し、教師用正解を表示しない。
- 解答付き：生徒用解答欄を非表示にし、問題直後に教師用正解を表示する。
- 問題＋解答：「問題のみ」の全ページに続けて、「解答付き」を新しいページから表示する。

### 15.2 倍率

- 25～200%を5%刻みで拡大・縮小する。
- 初期値は100%とする。
- 「幅に合わせる」はプレビューペインの表示可能な横幅から倍率を自動計算し、ペイン幅の変更へ追従する。
- 「ページ全体」はプレビューペインの表示可能な幅と高さから倍率を自動計算し、ページ全体が収まる倍率へ追従する。
- 自動計算した同一倍率を表示中の全ページへ適用する。
- 倍率中央の表示ボタンから直接数値を入力・選択する機能はない。

### 15.3 ページ分割方式

- ブラウザで実測した高さをもとに固定高ページへ配置する。
- 分割単位はトップレベルの各コンテンツと問題単位の正解・解説である。
- 現在ページに収まらない単位は次ページへ送る。
- 問題が複数単位に分かれた場合、問題番号は最初の単位だけに表示する。
- 先頭ページはヘッダー分だけ印刷可能高さを減らす。
- 明示改ページ、`pageBreakBefore`、`pageBreakAfter`を反映する。

段落、リスト項目、小問、解答欄、囲み枠、表、画像の内部分割は行わない。1つの分割単位が空ページの高さを超える場合も縮小せず配置するため、`.paper-page`の`overflow: hidden`により下部が切れることがある。

### 15.4 更新

編集状態の変更でReactプレビューを再描画する。画面には変更後50msの間「更新中…」を表示するが、プレビュー更新そのものを50ms遅延させる仕組みではない。

## 16. PDF出力

- 出力前に未保存変更を保存する。
- プレビューのページ分割完了を最大10秒待つ。
- 各ページDOMを4倍のピクセル比でPNGへ変換する。
- PNGをA4またはJIS B5のPDFページ全面へ配置する。
- プレビューとPDFは同じDOM描画を使用する。
- ファイル名は`題名_モード_YYYYMMDD-HHmm.pdf`とする。

PDFはラスタ画像PDFであり、文字検索、文字選択、コピー、アクセシビリティ用の文書構造を提供しない。

## 17. 自動保存とUndo / Redo

### 17.1 自動保存

- 編集変更は最後の変更から750ms後にIndexedDBへ保存する。
- 一覧へ戻る前とPDFダイアログを開く前は即時保存する。
- 保存失敗時はエラー表示と再試行操作を提供する。
- タブを直接閉じる直前の同期保存、`beforeunload`警告、別タブ編集ロックは実装しない。

### 17.2 Undo / Redo

- Worksheetの変更履歴をImmer patchとして保持する。
- Undo履歴は最大100件とする。
- 新しい変更を行うとRedo履歴を破棄する。
- 履歴はメモリ内だけに保持し、画面離脱・再読み込み後は復元しない。
- ヘッダーボタン、フォーカスが入力欄外の場合のCtrl＋Z、Ctrl＋Y、Ctrl＋Shift＋Zを提供する。
- WYSIWYG編集中はTipTap側の標準編集履歴が動作する。

## 18. ゴミ箱

- 一覧から移動する前に題名付きの確認画面を表示する。
- 移動後の通知から直後の操作を取り消せる。
- ゴミ箱では削除日時の降順に表示する。
- 1件ずつ復元、完全削除できる。
- ゴミ箱を空にできる。
- 完全削除時は参照画像も削除する。
- 一定期間後の自動削除は行わない。

## 19. JSONバックアップとインポート

### 19.1 ファイル形式

```json
{
  "format": "math-worksheet",
  "kind": "single | archive",
  "version": 1,
  "exportedAt": "ISO 8601 datetime"
}
```

- 単一形式は`worksheet`、全体形式は`worksheets`を持つ。
- 画像は`assets[].dataBase64`へdata URL接頭辞なしのBase64で格納する。
- 全体バックアップは通常一覧だけを対象とし、ゴミ箱を含めない。
- 参照されている画像だけを出力し、余剰画像を含めない。

### 19.2 検証

- JSONはZodスキーマで厳密に検証する。
- version 1だけを受け付ける。
- ID重複、画像参照欠落、WorksheetとAssetの所有関係、余剰Assetを拒否する。
- インポートUIは100MiBを超えるファイルを拒否する。
- 1件でも不正な場合は保存処理を開始しない。

バックアップ画像はMIME列挙、Base64形式、正の幅・高さを検証するが、インポート時に画像バイトを再デコードせず、10MiB・10,000px・40MPの挿入時上限を再検証しない。

### 19.3 取込動作

- 既存データを削除、置換、マージせず、すべて追加する。
- Worksheet、配下エンティティ、Assetへ新しいIDを付与する。
- 作成日時と更新日時を取込時刻へ変更する。
- 全件を1つのIndexedDBトランザクションで保存する。
- 取込後にプリント総数が2,000件を超える場合は保存しない。

## 20. 永続データと構造上限

| 項目 | 上限 |
|---|---:|
| IndexedDB内プリント数 | 2,000 |
| 1プリントの問題数 | 200 |
| 1問題のトップレベルコンテンツ数 | 100 |
| 1小問グループの小問数 | 100 |
| 表の行数 | 20 |
| 表の列数 | 20 |
| 1リッチテキスト文書のノード数 | 10,000 |
| リッチテキスト深さ | 20 |
| LaTeX文字数 | 5,000 |
| スペーサー行数 | 20 |
| 解答欄行数 | 20 |
| 題名文字数 | 100 |

永続化前にWorksheetスキーマを検証する。エンティティIDはWorksheet内で一意とし、ヘッダー題名はWorksheet題名と一致させる。

## 21. 主要技術と責務

| 領域 | 技術・方式 |
|---|---|
| UI | React 19、React Router 7 |
| 状態管理 | Zustand、Immer patch |
| リッチテキスト | TipTap 3 |
| 数式入力・表示 | MathLive |
| 永続化 | Dexie 4 / IndexedDB |
| 検証 | Zod 4、生成JSON Schema |
| PDF | html-to-image、React PDF |
| ビルド | TypeScript 5.9、Vite 8 |
| テスト | Vitest、Testing Library、fake-indexeddb |

## 22. 既知の制約

1. 問題・小問のドラッグ＆ドロップ並べ替えは未実装である。
2. 小問の複製、並べ替え、教師用正解編集は未実装である。
3. 段落の左・中央・右揃えを変更するUIは未実装である。
4. 問題の正解・解説へ数式、画像、表を挿入するUIは未実装である。
5. 選択した日本語フォントをプレビューとPDFへ反映する処理は未実装である。
6. コンテンツ内部のページ分割と特大ブロックの縮小は未実装である。
7. PDFはラスタ画像であり、検索可能なテキストを持たない。
8. バックアップ取込時の画像バイト再検証は未実装である。
9. タブを直接閉じる直前の保存保証と同一プリントの複数タブ編集制御はない。

## 23. 受け入れ条件

現行版は最低限、次を満たすこと。

1. ChromeまたはEdgeで新規プリントを作成し、再読み込み後もIndexedDBから開ける。
2. 題名検索、複製、ゴミ箱移動、復元、完全削除ができる。
3. JIS B5とA4、4段階の余白、6種類の問題番号を設定できる。
4. 問題を1～200件の範囲で追加、複製、削除、上下移動できる。
5. 採番なしと任意番号からの振り直しがプレビューへ反映される。
6. 本文、囲み枠、小問、解答欄、スペーサー、改ページを編集できる。
7. 本文系エディタで文字装飾、リスト、数式、画像、表を保存・再編集できる。
8. 小問の追加、削除、半幅／全幅、採番振り直しができる。
9. 一般表、関数表、度数分布表を挿入し、行列、結合、行高、列幅、セル文字、セル数式を編集できる。
10. 問題のみ、解答付き、問題＋解答のプレビューを表示できる。
11. 明示改ページとコンテンツ単位の自動改ページがPDFにも一致する。
12. A4またはJIS B5のラスタ画像PDFをダウンロードできる。
13. 編集変更が750msのdebounce後に自動保存され、保存状態が表示される。
14. 最大100件のUndo / Redoが編集セッション中に機能する。
15. 単一プリントと通常一覧全体をJSONへ出力できる。
16. version 1の有効な単一／全体JSONを、新しいIDで追加インポートできる。
17. 不正なJSONは既存データを変更せず拒否される。
18. `npm run verify`と`npm run build`が成功する。

## 24. 結論

現行システムは、構造化した数学プリントをPCブラウザで編集し、端末内保存、3モードのページプレビュー、ラスタ画像PDF、JSONバックアップを提供するローカルWebアプリケーションとする。

今後の機能追加では、本書の「既知の制約」を解消した時点で該当記述と受け入れ条件を更新し、Zodスキーマ、生成JSON Schema、テストとの整合を維持する。
