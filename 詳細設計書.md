# 数学プリント作成ソフト 詳細設計書（現行実装準拠版）

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 数学プリント作成ソフト 詳細設計書 |
| 文書版 | 1.1（現行実装準拠） |
| データスキーマ版 | 1 |
| 基準日 | 2026-08-12 |
| 対象コード | `src/`、`scripts/`、`schemas/` |
| 対象端末 | 横幅1024px以上のPC |
| 対応ブラウザ | Google Chrome、Microsoft Edge |
| 永続先 | ブラウザ内IndexedDB、UI設定のみLocalStorage |
| バックエンド | なし |

本書は、現行コードの構造と処理を保守・改修できる粒度で記述する。将来構想ではなく、コードに存在する処理を設計の正本とする。

### 1.1 参照資料と優先順位

1. 永続データは`src/domain/worksheet/worksheet.schema.ts`を正本とする。
2. 実際の画面動作は`src/presentation/`の実装を正本とする。
3. 保存、バックアップ、PDFの処理は`src/application/`と`src/infrastructure/`を正本とする。
4. 業務要件と受け入れ条件は`要件定義書.md`を参照する。
5. 本書とコードが異なる場合はコードを優先し、本書を更新する。

生成物である`schemas/math-worksheet.schema.json`は直接編集しない。

### 1.2 対象外

- サーバーAPI、認証、クラウド同期
- スマートフォン用編集UI
- PWA、Service Worker、オフライン配信保証
- 共同編集、複数タブ競合制御
- 旧スキーマのマイグレーション
- 作図、グラフ、ページ番号

## 2. システム構成

### 2.1 実行方式

本システムはViteで構築するReact SPAである。`src/main.tsx`がReact Strict Modeで`App`をマウントし、React Routerの`BrowserRouter`で画面を切り替える。

```text
Browser
├─ React Presentation
│  ├─ 一覧 / ゴミ箱
│  ├─ 編集 + Zustand Store
│  ├─ TipTap / MathLive
│  └─ HTMLページプレビュー
├─ Domain
│  ├─ Zod Schema / Type
│  ├─ Worksheet Command
│  ├─ 採番 / 表操作
│  └─ 既定値 / 上限値
├─ Application
│  ├─ Repository契約
│  ├─ JSONバックアップ
│  └─ PDF生成
└─ Infrastructure
   ├─ Dexie / IndexedDB
   └─ Blob / Object URLダウンロード
```

Worksheetを編集データの中心に置き、プレビュー、PDF、JSONを派生物として生成する。

### 2.2 レイヤー責務

| レイヤー | ディレクトリ | 責務 |
|---|---|---|
| Domain | `src/domain/worksheet/` | 永続Schema、型、既定値、純粋な更新処理、採番、表操作、構造上限 |
| Application | `src/application/` | Repository契約、バックアップ変換、PDF生成ユースケース |
| Infrastructure | `src/infrastructure/` | Dexie実装、IndexedDBトランザクション、ファイルダウンロード |
| Presentation | `src/presentation/` | 画面、ダイアログ、エディタ、状態管理、プレビュー |

PresentationからのWorksheet更新は、原則としてDomain Commandが返す新しいWorksheetをZustandへcommitする。Repositoryは保存境界で再度Zod検証を行う。

### 2.3 主要ライブラリ

| 用途 | ライブラリ |
|---|---|
| UI | React 19、React DOM 19 |
| ルーティング | React Router DOM 7 |
| 編集状態 | Zustand 5、Immer 11 |
| リッチテキスト | TipTap 3、StarterKit |
| 数式 | MathLive 0.110 |
| 永続化 | Dexie 4 |
| 検証 | Zod 4 |
| PDF | html-to-image、React PDF 4 |
| アイコン | Lucide React |
| ビルド | TypeScript 5.9、Vite 8 |
| テスト | Vitest 4、Testing Library、fake-indexeddb、Playwright 1 |

ドラッグ＆ドロップおよびDOMPurifyを前提とした設計にはしない。未使用の`@dnd-kit/*`と`dompurify`は依存関係から除外する。

## 3. 起動、環境判定、ルーティング

### 3.1 起動処理

`src/main.tsx`は次を行う。

1. `mathlive/fonts.css`を読み込む。
2. `mathlive/static.css`を読み込む。
3. アプリ共通の`styles.css`を読み込む。
4. `#root`へ`<React.StrictMode><App /></React.StrictMode>`を描画する。

### 3.2 必須API判定

`App.supportsRequiredApis()`はテストモード以外で次を確認する。

- `indexedDB`
- `Blob`
- `URL.createObjectURL`
- `createImageBitmap`

不足時はルーターを起動せず、「このブラウザは対応していません」を表示する。現行コードが利用しない`BroadcastChannel`は起動条件に含めない。

### 3.3 ルート

| URL | React要素 | ファイル |
|---|---|---|
| `/` | `WorksheetListScreen` | `presentation/worksheet-list/WorksheetListScreen.tsx` |
| `/worksheets/:worksheetId` | `EditorScreen` | `presentation/editor/EditorScreen.tsx` |
| `/trash` | `TrashScreen` | `presentation/trash/TrashScreen.tsx` |
| その他 | `/`へ`Navigate replace` | `App.tsx` |

## 4. ソース構成

### 4.1 Domain

| ファイル | 主な責務 |
|---|---|
| `worksheet.schema.ts` | Zodスキーマ、相関検証、バックアップ検証 |
| `worksheet.ts` | `z.infer`で導出した型の公開窓口 |
| `worksheet.defaults.ts` | ID、空文書、Worksheet、各ブロックの生成 |
| `worksheet.commands.ts` | Worksheetを不変更新する操作関数 |
| `worksheet.numbering.ts` | 問題・小問の採番と表示形式 |
| `worksheet.search.ts` | 題名検索キーの正規化 |
| `rich-text.ts` | プレーンテキスト変換、可視内容判定 |
| `table-operations.ts` | 表の論理グリッド編集、行高・列幅変更 |
| `page-tokens.ts` | 用紙寸法、余白、mmからptへの変換 |
| `structure-limits.ts` | Schema、Command、UIで共有する上限 |

### 4.2 Application / Infrastructure

| ファイル | 主な責務 |
|---|---|
| `application/repositories/worksheet-repository.ts` | Repositoryインターフェース |
| `application/backup/backup.ts` | Blob/Base64変換、単一・全体JSON、ID再採番 |
| `application/pdf/generate-pdf.tsx` | プレビューページをPNG化してPDFへ格納 |
| `infrastructure/indexeddb/database.ts` | Dexie DBとテーブル定義 |
| `infrastructure/indexeddb/dexie-worksheet-repository.ts` | RepositoryのDexie実装 |
| `infrastructure/file/download.ts` | ファイル名正規化、Blob URL生成・破棄 |

### 4.3 Presentation

| ファイル | 主な責務 |
|---|---|
| `worksheet-list/WorksheetListScreen.tsx` | 一覧、検索、ページング、JSON入出力 |
| `trash/TrashScreen.tsx` | ゴミ箱、復元、完全削除 |
| `editor/EditorScreen.tsx` | 読込、自動保存、ペイン、プレビュー、PDF導線 |
| `editor/editor-store.ts` | Worksheet状態、revision、Undo / Redo |
| `editor/ProblemCard.tsx` | 問題、コンテンツ、小問、独立表の編集 |
| `components/RichTextEditor.tsx` | TipTap本体と書式ツールバー |
| `components/rich-text-editor-extensions.ts` | 数式、画像、表、文字サイズの拡張と正規化 |
| `components/TableStructureToolbar.tsx` | 行列、結合、行高、列幅操作 |
| `components/MathFormula.tsx` | MathLive SSRによる数式HTMLと読み上げ名 |
| `preview/WorksheetPreview.tsx` | RenderAtom生成、DOM計測、ページ描画 |
| `preview/pagination.ts` | 実測アイテムを固定高ページへ割り付け |
| `dialogs/EditorDialogs.tsx` | 設定、PDF、数式、表、画像ダイアログ |
| `components/Modal.tsx` | モーダル、Esc、簡易フォーカストラップ |
| `components/Toast.tsx` | 状態通知と任意のアクション |
| `app/ui-preferences.ts` | ペイン比率、倍率、表示モードのLocalStorage保存 |

## 5. 永続データ設計

### 5.1 Schemaの正本

`worksheet.schema.ts`を唯一の永続データ定義とする。

```text
Zod Schema
├─ z.infer → TypeScript型（worksheet.ts）
└─ z.toJSONSchema → schemas/math-worksheet.schema.json
```

すべてのオブジェクトは原則`z.strictObject`であり、未定義プロパティを拒否する。現在の`CURRENT_SCHEMA_VERSION`は`1`、ファイル識別子は`math-worksheet`である。

### 5.2 共通プリミティブ

| 型 | 制約 |
|---|---|
| `EntityId` | 1文字以上の文字列 |
| `ISODateTimeString` | タイムゾーンを含むISO 8601日時 |
| `SchemaVersion` | リテラル`1` |
| Worksheet題名 | 1～100文字、空白以外を1文字以上含む |

通常のID生成は`crypto.randomUUID()`を使用する。利用できない環境では時刻と乱数を含む`id-*`文字列へフォールバックする。

### 5.3 Worksheet

```ts
type Worksheet = {
  schemaVersion: 1;
  id: string;
  title: string;
  pageSettings: PageSettings;
  header: WorksheetHeader;
  problems: ProblemBlock[];
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
};
```

相関制約は次のとおり。

- `header.title === title`
- Worksheet配下のすべての`id`は一意
- `problems`は1～200件
- `deletedAt === null`を通常一覧、非nullをゴミ箱として扱う。

### 5.4 用紙とヘッダー

```ts
type PageSettings = {
  size: "A4" | "B5";
  orientation: "portrait";
  margin: "wide" | "normal" | "narrow" | "veryNarrow";
  fontFamily:
    | "biz-udp-gothic"
    | "biz-ud-gothic"
    | "biz-udp-mincho"
    | "noto-sans-jp"
    | "noto-serif-jp";
  problemNumberFormat:
    | "plain" | "dot" | "rightParen"
    | "paren" | "bracket" | "question";
};
```

`WorksheetHeader`は題名、年、組、番、名前の表示フラグと`firstPageOnly: true`を持つ。用紙寸法と余白は`page-tokens.ts`で次のように定義する。

| 設定 | 値 |
|---|---|
| A4 | 210×297mm |
| JIS B5 | 182×257mm |
| wide | 20mm |
| normal | 15mm |
| narrow | 10mm |
| veryNarrow | 5mm |

`mmToPt(mm) = mm * 72 / 25.4`とする。

### 5.5 ProblemBlock

```ts
type ProblemBlock = {
  id: string;
  type: "problem";
  numbering: {
    enabled: boolean;
    restartAt: number | null;
  };
  contents: ContentBlock[];
  solution: SolutionRichTextDocument | null;
  pageBreakBefore: boolean;
  pageBreakAfter: boolean;
};
```

`contents`は0～100件である。現行UIは`pageBreakBefore`と`pageBreakAfter`を変更しないが、インポートした値とプレビュー処理はこれらを保持・反映する。

### 5.6 ContentBlock

| `type` | 主なフィールド | 用途 |
|---|---|---|
| `richText` | `document` | 本文 |
| `box` | `title`、`preset`、`document` | 囲み枠 |
| `subQuestionGroup` | `numbering`、`columns`、`items` | 小問群 |
| `answerArea` | `style`、`rows` | 独立解答欄 |
| `spacer` | `rows` | 空白 |
| `image` | `assetId`、配置、幅、代替文字 | 独立画像参照 |
| `table` | 行、列幅、見出し行 | 独立表 |
| `pageBreak` | IDのみ | 明示改ページ |

囲み枠の`preset`は`simple`、`heading`、`band`、`emphasis`である。解答欄は`lines`または`box`、高さは1～20行とする。

### 5.7 小問

```ts
type SubQuestion = {
  id: string;
  numbering: { restartAt: number | null };
  content: BasicRichTextDocument;
  answerArea: AnswerArea | null;
  solution: SolutionRichTextDocument | null;
  width: "column" | "full";
};
```

小問グループは1～100件の小問を持つ。番号形式は`paren`、`dot`、`circled`、`kana`を保存でき、列数は`1 | 2`を保存できる。現行の生成値は`format: "paren"`、`columns: 2`であり、UIは番号形式と列数を変更しない。表示幅は各小問の`width`で決定する。

### 5.8 リッチテキストAST

保存データはHTML文字列ではなく、制限されたJSON ASTである。

```text
doc
├─ paragraph
│  ├─ text + marks
│  ├─ hardBreak
│  └─ inlineMath
├─ bulletList / orderedList
├─ blockMath
├─ imageRef
├─ richTable
└─ spacer（Solutionだけ）
```

許可するmarkは`bold`、`underline`、`italic`、`textSize`である。標準文字サイズはmarkを付けず、小、大、特大だけを`textSize`として保存する。同一種類のmark重複を拒否する。

用途別プロファイルは次のとおり。

| 文書型 | 許可するトップレベルノード |
|---|---|
| `BasicRichTextDocument` | 段落、リスト、独立数式、画像、表 |
| `SolutionRichTextDocument` | Basicの内容にスペーサーを追加 |
| `TableCellRichTextDocument` | 段落、画像。段落内に文字、改行、行内数式 |

表セルへ`richTable`を入れられないため、表の入れ子はSchemaで拒否する。

リッチテキスト全体は最大10,000ノード、深度20とする。未知のノード、未知の属性、自由なstyle属性を拒否する。

### 5.9 数式値

数式ノードは`latex`と`textSize`を必須とする。LaTeXは1～5,000文字で、空白だけを拒否する。次の外部参照またはHTML/CSS操作コマンドを正規表現で拒否する。

`href`、`url`、`includegraphics`、`htmlClass`、`htmlId`、`htmlStyle`、`htmlData`、`cssId`、`cssClass`、`class`、`style`

### 5.10 画像参照とAsset

独立画像の`ImageBlock`とリッチテキスト内の`imageRef`は同じ参照情報を持つ。

```ts
type ImageReference = {
  id: string;
  assetId: string;
  alt: string;
  placement: "block" | "floatLeft" | "floatRight";
  widthPercent: 25 | 33 | 50 | 66 | 75 | 100;
};
```

回り込み時は25、33、50だけを許可する。画像本体は次の`AssetRecord`として別テーブルへ保存する。

```ts
type AssetRecord = {
  id: string;
  worksheetId: string;
  mimeType: "image/png" | "image/jpeg" | "image/webp";
  blob: Blob;
  width: number;
  height: number;
  createdAt: string;
};
```

### 5.11 表

```ts
type TableBlock = {
  id: string;
  type: "table";
  rows: TableRow[];
  columnWidthsPercent: number[];
  headerRow: boolean;
};
```

表の制約は次のとおり。

- 1～20行、1～20列
- 列幅は正数かつ各100以下、合計100±0.01
- 行高は省略時自動、指定時5～100mm
- `rowSpan`と`columnSpan`は正の整数
- 結合範囲が論理グリッド外へ出ないこと
- 結合範囲同士が重複しないこと
- 論理グリッドに未定義セルが残らないこと

### 5.12 構造上限

| キー | 値 |
|---|---:|
| `worksheetsPerArchive` | 2,000 |
| `problemsPerWorksheet` | 200 |
| `contentBlocksPerProblem` | 100 |
| `subQuestionsPerGroup` | 100 |
| `tableRows` | 20 |
| `tableColumns` | 20 |
| `richTextNodesPerDocument` | 10,000 |
| `richTextDepth` | 20 |
| `latexCharacters` | 5,000 |
| `spacerRows` | 20 |
| `answerAreaRows` | 20 |

## 6. 既定値とDomain Command

### 6.1 Worksheet生成

`createWorksheet()`は次を生成する。

- 題名：`無題のプリント`
- JIS B5、縦、標準余白、BIZ UDPゴシック設定値
- 問題番号：`dot`
- 年、組、番、名前：すべて表示
- 空の本文を1件持つ問題：1件
- `deletedAt: null`

### 6.2 ブロック生成

| 関数 | 生成内容 |
|---|---|
| `createRichTextBlock` | 空の左揃え段落を持つ本文 |
| `createBoxBlock` | 空題名、`simple`、空本文 |
| `createAnswerAreaBlock` | 横罫線2行 |
| `createSpacerBlock` | 2行 |
| `createPageBreakBlock` | 改ページ |
| `createSubQuestion` | 半幅、横罫線1行、正解なし |
| `createSubQuestionGroup` | `(1)`形式、2列、小問2件 |
| `createTableBlock` | 指定テンプレートの表 |

### 6.3 Commandの戻り値

構造操作は次の判別Unionを返す。

```ts
type WorksheetCommandResult =
  | { ok: true; worksheet: Worksheet }
  | {
      ok: false;
      worksheet: Worksheet;
      code: "STRUCTURE_LIMIT_EXCEEDED" | "NOT_FOUND" | "LAST_ITEM";
    };
```

成功時は`structuredClone`したWorksheetを変更し、`updatedAt`を現在時刻へ更新する。元のWorksheetは変更しない。

### 6.4 主要Command

| 関数 | 処理 |
|---|---|
| `setWorksheetTitle` | trim、空欄補正、100文字切詰め、ヘッダー同期 |
| `applyWorksheetSettings` | 用紙設定とヘッダーを複製して適用 |
| `addProblem` | 指定問題の直後へ追加 |
| `deleteProblem` | 最後の1問を除き削除 |
| `duplicateProblem` | 配下の全`id`を再生成して直後へ複製 |
| `moveProblem` | 指定indexへ移動 |
| `addContent` | 選択コンテンツ直後、なければ末尾へ追加 |
| `updateRichTextDocument` | 本文、囲み枠、小問本文を対象指定で更新 |
| `updateImageReference` | 独立画像または文書内画像を更新し、回り込み幅を最大50へ補正 |
| `moveContent` | 1件上または下へ移動 |
| `setProblemSolution` | 問題単位の正解・解説を設定 |
| `addSubQuestion` | グループ末尾へ追加 |
| `deleteSubQuestion` | 最後の1件を除き削除 |
| `cloneWorksheetWithNewIds` | 全ID、題名、日時、削除状態を複製用に再構築 |

問題複製時は画像の`assetId`を維持する。プリント複製時はRepositoryがAsset自体も複製し、その後参照先を新しいAsset IDへ置換する。

### 6.5 採番

問題番号は先頭から走査し、`enabled === false`なら`null`を設定してカウントしない。有効な問題では`restartAt ?? current + 1`を使用する。

小問も同じく`restartAt ?? current + 1`で採番する。丸数字は1～20、カナは定義済み文字列の範囲を使用し、範囲外は`(n)`へフォールバックする。

### 6.6 検索正規化

`normalizeSearchKey()`は次の順で処理する。

1. Unicode NFC正規化
2. 全角ASCII（U+FF01～U+FF5E）を半角へ変換
3. 全角空白を半角空白へ変換
4. `ja-JP`ロケールで小文字化
5. 前後空白を除去

内部空白は圧縮せず、ひらがなとカタカナは変換しない。

## 7. IndexedDBとRepository

### 7.1 データベース

- DB名：`math-worksheet-db`
- Dexie version：1

| テーブル | 主キー・索引 |
|---|---|
| `worksheets` | `id, updatedAt, deletedAt, title` |
| `assets` | `id, worksheetId, createdAt` |
| `editLocks` | `worksheetId, ownerTabId, heartbeatAt` |

`editLocks`型とテーブルは存在するが、現行実装は取得、heartbeat、解放を行わない。複数タブロック機能としては動作しない。

### 7.2 Repository契約

`WorksheetRepository`は次を公開する。

- `list`
- `get`
- `count`
- `create`
- `createMany`
- `save`
- `trash`
- `restore`
- `deletePermanently`
- `emptyTrash`
- `duplicate`
- `putAsset`

### 7.3 読込

`list()`は全Worksheetを読み、1件ずつ`WorksheetSchema.safeParse`する。正常データは返却し、不正データは削除せず`invalidCount`へ加算する。

`get(id)`はWorksheetを`parse`し、同じ`worksheetId`のAssetをすべて取得して`AssetRecordSchema.parse`する。不正データの場合は例外を呼出元へ返す。

### 7.4 作成・保存

`create()`と`createMany()`はWorksheetとAssetを先にZod検証し、`worksheets`と`assets`を単一のread-writeトランザクションで保存する。プリント数はゴミ箱を含め2,000件までで、超過時は`WorksheetLimitError`を投げる。

`save()`はWorksheetを検証して`put`する。Assetは変更しない。

`putAsset()`はAssetと更新後Worksheetを検証し、同一トランザクションで両方を`put`する。

### 7.5 ゴミ箱

`trash()`と`restore()`は`deletedAt`を更新し、同時に`updatedAt`も更新する。

`deletePermanently()`はWorksheet IDに属するAssetを先に削除し、Worksheetを削除する。`emptyTrash()`は`deletedAt !== null`の全Worksheetについて同じ処理を行い、削除件数を返す。

### 7.6 プリント複製

1. 元WorksheetとAssetを取得する。
2. `cloneWorksheetWithNewIds()`でWorksheetと配下エンティティを複製する。
3. Assetごとに新しいID、複製先Worksheet ID、現在日時を付与する。
4. Worksheet全体を再帰走査し、`assetId`を新しいIDへ置換する。
5. `create()`でWorksheetとAssetを保存する。

画像差し替えでは新しいAssetを追加する。差し替え前AssetとObject URLはUndo/Redoで復元できる間だけ保持し、履歴から外れたObject URLを解放する。通常の編集終了時は最新Worksheetの保存と未参照Asset削除を同一トランザクションで行う。未参照Assetは単一・全体バックアップからも除外する。

## 8. 編集状態、自動保存、UI設定

### 8.1 Zustand Store

`useEditorStore`は次を保持する。

| 状態 | 内容 |
|---|---|
| `worksheet` | 編集中Worksheetまたはnull |
| `sessionId` | Worksheet初期化・clearごとに増加する編集セッション識別子 |
| `revision` | 変更、Undo、Redoのたびに増加 |
| `savedRevision` | 保存済み最大revision |
| `saveStatus` | `saved | dirty | saving | failed` |
| `selectedProblemId` | 選択問題 |
| `selectedContentId` | 選択コンテンツ |
| `undoStack` | 最大100件 |
| `redoStack` | Redo候補 |

### 8.2 Commit

低頻度の構造Commandは`commit(label, nextWorksheet)`へ完成済みWorksheetを渡す。参照同一、またはImmer patchが空なら何もしない。Worksheet全体を`JSON.stringify`する比較は行わない。

題名、TipTap本文、囲み枠、めあて、解答欄、教師用解説、小問、表セルなどの高頻度更新は`mutate(label, recipe, options)`を使用する。`produceWithPatches`のdraft上で対象Problem / Contentだけを変更するため、変更していないProblemとContentの参照は維持される。実データのpatchが空の場合は`updatedAt`だけを変更せず終了する。

- `revision += 1`
- `saveStatus = dirty`
- Undoへ履歴追加、直近100件だけ保持
- Redoを空にする。

同じ`historyGroup`への更新間隔が1,000ms以内なら、patchを順方向、inverse patchを逆方向へ連結し、1件のUndo履歴へまとめる。編集対象の変更または1,000msを超える中断で新しい履歴を開始する。revisionと自動保存判定は入力更新ごとに増加する。

### 8.3 入力性能

`npm run benchmark:editor`は、100問題、各20コンテンツ、各10段落のデータで次を比較する。

- 旧方式：Worksheet全体の`structuredClone`、`JSON.stringify`×2、Immer patch生成
- 現方式：対象Problem / ContentだけのImmer mutation

ベンチマークは環境依存の参考値であり、CIの合否条件にはしない。部分更新の構造共有とUndo結合は`editor-store.test.ts`で決定的に検証する。

### 8.4 Undo / Redo

Undoはinverse patch、Redoはpatchを`applyPatches`で適用する。どちらもrevisionを増加させ、未保存状態にする。履歴はメモリだけにあり、編集画面のアンマウント時に破棄する。

画面全体のCtrl＋Z、Ctrl＋Y、Ctrl＋Shift＋Zは、イベント発生元が`input`、`textarea`、`contenteditable`内の場合には処理しない。リッチテキスト内ではTipTap側の編集履歴を使用する。

### 8.5 自動保存

`EditorScreen`は`saveStatus === dirty`のとき750msタイマーを開始する。保存要求は`worksheetId`、`sessionId`、`revision`を保持する。`initialize()`と`clear()`はセッション番号を更新し、保存完了・失敗は同じWorksheetとセッションに属する要求だけをStoreへ反映する。古いrevisionの保存完了後に新しい編集があれば状態を`dirty`へ戻す。

保存中に新しいrevisionが発生した場合、完了後も最新revisionとの差により`dirty`へ戻る。

`flushSave()`は次の前に即時実行する。

- 一覧へ戻る
- PDFダイアログを開く

`dirty`、`saving`、`failed`状態では`beforeunload`で離脱警告を表示する。IndexedDBを同期保存できないためタブ終了直前の保存完了は保証しない。通常のReactルート離脱では最新Worksheetを保存し、未参照Assetを削除する。

### 8.6 UI Preferences

LocalStorageキーは`math-worksheet:ui-preferences:v1`である。

```ts
type UiPreferences = {
  version: 1;
  paneRatio: number; // 0.35～0.65
  zoom: number | "fitWidth" | "fitPage"; // numberは0.25～2.0、0.05刻み
  previewMode: "questions" | "withAnswers" | "questionsAndAnswers";
};
```

初期値はペイン比率`704 / 1440`、倍率100%、問題のみである。不正JSONや保存失敗は例外を外へ出さず、既定値または`false`を返す。

## 9. プリント一覧画面

### 9.1 初期読込

マウント時に`worksheetRepository.list()`を呼ぶ。状態は`loading`、`error`、`worksheets`、`invalidCount`で管理する。

- 読込中：3行のスケルトン
- 失敗：メッセージと再読込ボタン
- 不正データあり：削除していない旨と件数を警告
- 0件：新規作成とインポートの空状態

### 9.2 並び順とページング

通常一覧は`deletedAt === null`を抽出し、`updatedAt`降順、同値ならID昇順で並べる。1ページ50件とし、表示範囲と前後ボタンを表示する。

### 9.3 検索

入力値を150ms debounceして検索キーへ反映する。題名と検索語の両方へ`normalizeSearchKey()`を適用し、`includes`で部分一致する。検索変更時は1ページ目へ戻る。

### 9.4 新規作成

`createWorksheet()`を生成し、Asset空配列とともにRepositoryへ保存してから編集URLへ移動する。2,000件到達時はボタンを無効化する。

### 9.5 行メニュー

- 開く
- 複製
- JSONエクスポート
- ゴミ箱へ移動

ゴミ箱移動は確認後に実行し、完了Toastへ「元に戻す」を表示する。Toastの操作は`restore()`を呼び、一覧を再読込する。

## 10. ゴミ箱画面

マウント時に全Worksheetを読み、`deletedAt !== null`だけを削除日時降順で表示する。

- 復元：確認なしで`restore()`を実行
- 完全削除：確認Modal後に`deletePermanently()`
- ゴミ箱を空にする：件数を含む確認Modal後に`emptyTrash()`

完全削除後は取り消せない。ゴミ箱データの自動削除は行わない。

## 11. 編集画面

### 11.1 初期化

1. URLから`worksheetId`を取得する。
2. RepositoryからWorksheetとAssetを取得する。
3. データがない、または`deletedAt !== null`ならNot Found表示にする。
4. Storeを`initialize()`する。
5. Assetごとに`URL.createObjectURL(blob)`を生成し、`assetUrls` Mapへ保持する。
6. アンマウント時に最新Worksheetを保存して未参照Assetを削除し、Storeをclearして保持中のObject URLを破棄する。

### 11.2 ヘッダー

- 一覧：`flushSave()`成功後に遷移
- 題名：`maxLength=100`、変更ごとにcommit、blur時に空欄補正
- 保存状態：保存済み、未保存、保存中、失敗と再試行
- Undo / Redo：履歴がない場合は無効
- プリント設定：設定ダイアログ
- PDF出力：`flushSave()`成功後にPDFダイアログ

### 11.3 画面幅

`.editor-workspace`は最小幅1024pxである。CSS media queryが1023px以下の場合、編集ワークスペースとヘッダー主要操作を隠し、PC画面利用の警告を表示する。

### 11.4 ペイン

左ペイン比率は35～65%へ制限する。境界のポインター移動時は画面内座標から比率を求める。キーボードでは左右矢印2%刻み、Shift併用10%刻みで変更する。pointer up時にLocalStorageへ保存する。

### 11.5 プレビュー更新表示

Worksheet変更時に`previewUpdating`をtrueにし、50ms後にfalseへ戻す。Reactによるプレビュー描画自体はWorksheet変更と同時に開始され、50ms後まで遅延しない。

## 12. 問題・コンテンツ編集

### 12.1 ProblemCard

各問題は番号、再開状態、複製、設定、コンテンツ、教師用正解、内容追加を表示する。

問題設定メニューは次を持つ。

- 問題を複製
- 上へ移動
- 下へ移動
- 問題番号を付ける
- この問題から振り直す
- 開始番号
- 問題を削除

ドラッグハンドルは表示するが、イベントハンドラーは接続していない。移動はメニューボタンで行う。

### 12.2 内容追加

追加メニューは本文、囲み枠、小問、解答欄、スペーサー、改ページを表示する。画像と表は追加メニューへ表示せず、各リッチテキストエディタのツールバーから挿入する。

選択コンテンツの右上に上移動、下移動、削除を表示する。

### 12.3 小問編集

小問は2列CSS Gridで表示し、`width === full`なら2列分を占有する。次を編集できる。

- 本文
- 半幅／全幅
- 解答欄の種類と高さ
- 小問追加
- 小問削除
- 任意位置からの採番再開

小問の最後の1件は削除ボタンを無効化する。小問複製、上下移動、解答欄の有無変更、教師用正解編集は現行UIにない。

### 12.4 教師用正解・解説

折りたたみボタンで問題単位の`solution`エディタを表示する。nullの場合は空のSolution文書を画面用に生成する。現在の呼出しでは数式、画像、表のcallbackを渡さないため、ツールバーは文字装飾とリストだけを表示する。

## 13. RichTextEditor

### 13.1 TipTap構成

StarterKitから次を無効化する。

- blockquote
- code
- codeBlock
- heading
- horizontalRule
- link
- strike

追加Extensionは`ParagraphTextAlign`、`TextSize`、`InlineMath`、`BlockMath`、`ImageRef`、`RichTable`である。

### 13.2 ツールバー

- 文字サイズ：小、標準、大、特大
- 太字
- 下線
- 斜体
- 箇条書き
- 番号付きリスト
- 数式
- 画像
- 表

表示する挿入ボタンはPropsで決まる。表セル編集中はリスト、画像、表を無効または非表示にし、数式は行内だけを挿入する。段落揃えExtensionは存在するが、揃え変更ボタンはない。

### 13.3 外部値同期

`useEditor`のcontentへ保存文書を正規化して渡す。外部Worksheet更新時、エディタがフォーカス中でなければ現在JSONと次JSONを比較し、異なる場合だけ`emitUpdate: false`で置換する。

Asset URL Mapが変わるとTipTapインスタンスが再生成される。破棄済みインスタンスへコマンドを送らないよう`isDestroyed`を確認する。

### 13.4 保存用正規化

`normalizeEditorDocument()`はTipTap JSONから許可されたノードとmarkだけを抽出する。

- 未知ノードと未知markを捨てる。
- 空文字textを捨てる。
- 段落揃えをleft、center、rightへ限定する。
- 数式の空値を捨て、文字サイズを補正する。
- 画像配置と幅を許可値へ補正する。
- 空文書には空の左揃え段落を補う。

`normalizeTableCellEditorDocument()`はさらに段落と画像だけへ限定する。外部貼り付けはTipTapのSchemaとこの正規化を通る。`DOMPurify`を明示的に呼ぶ処理はない。

### 13.5 数式Node View

MathLive SSRの`convertLatexToMarkup()`でHTMLを生成し、`convertLatexToSpeakableText()`で`aria-label`を生成する。変換失敗時はLaTeX文字列をフォールバック表示する。

エディタ内の数式Node Viewはhover、選択、focus時に編集ボタンを表示する。編集ダイアログでは元の行内／独立形式を維持し、LaTeXと文字サイズだけを変更する。

### 13.6 画像Node View

`assetUrls.get(assetId)`を`img.src`へ設定する。参照先がない場合は欠落表示にする。編集callbackがある場合は画像上へ編集ボタンを表示し、現在のID、Asset ID、配置、幅、代替文字を返す。

### 13.7 RichTable Node View

表は選択中の1セルだけを編集可能にし、他セルは完成形プレビューを表示する。各セルの更新を表ノードの`rows`へ反映する。

選択セルに対し、行列操作、結合、分割、行高、列幅、行内数式を提供する。別セル選択時は前のセルコントローラーをdeactivateする。

## 14. 数式・画像・表ダイアログ

### 14.1 数式

MathLiveの`math-field`を動的import後に使用する。初期値は`2x + 3 = 9`、標準サイズ、行内である。パレットは次を挿入する。

`+`、`-`、`\times`、`\div`、`=`、`\ne`、`\pm`、`\frac{}{}`、`\sqrt{}`、`x^2`、`\le`、`\ge`

表セル用の`inlineOnly`では独立数式選択を表示せず、必ず行内数式として返す。

### 14.2 画像

画像選択時の手順は次のとおり。

1. MIME型がPNG、JPEG、WebPか確認する。
2. 10MiB以下か確認する。
3. `createImageBitmap()`でデコードする。
4. 各辺10,000px以下、総画素40MP以下か確認する。
5. `AssetRecord`を生成する。

配置は独立、左回り込み、右回り込みである。幅は25、33、50、66、75、100%で、回り込み時は50%超の選択肢を無効化する。

編集時はファイル未選択でも既存Assetのまま配置、幅、代替文字を変更できる。ファイルを選んだ場合だけ新しいAssetを返す。

### 14.3 表

テンプレートは一般、関数、度数分布である。

- 一般：1～20行、1～20列を指定
- 関数：行数を2へ固定し、列数は現在の列数stateを使う。初期状態では2行×4列。先頭列へx、y
- 度数分布：列数を2へ固定し、行数は現在の行数stateを使う。初期状態では3行×2列。先頭行へ階級、度数

初期画面値は3行×4列である。行数・列数入力は一般テンプレート選択時だけ表示するが、一般で変更後に別テンプレートへ切り替えた場合は、そのstateを上記の可変側へ引き継ぐ。

### 14.4 表操作

`table-operations.ts`は論理グリッドを構築して操作可否を判定する。

- 行を上／下へ追加
- 行削除
- 列を左／右へ追加
- 列削除
- 右セルと横結合
- 下セルと縦結合
- 結合セルを分割

結合時は両セルの文書内容を保持する。分割時は左上セルへ内容を残し、新しいセルは空文書とする。20行または20列では追加操作を無効化する。

行高はnullまたは5～100mm、列幅は1～99%とする。列幅変更時は他列を比例調整し、合計100を維持する。1列だけの表では列幅変更を無効化する。

## 15. ModalとToast

### 15.1 Modal

- `role="dialog"`、`aria-modal="true"`
- 表示時にダイアログへfocus
- Escapeで閉じる
- Tab / Shift+Tabで先頭と末尾を循環
- 背景自体のmousedownで閉じる
- 閉じた後に表示前の要素へfocusを戻す
- small、medium、largeの3サイズ

同一画面に複数Modalを重ねる設計は採らない。タイトルIDは固定`modal-title`である。

### 15.2 Toast

`role="status"`としてメッセージを表示する。任意のアクションボタンと閉じるボタンを持つ。自動消去タイマーはなく、利用者操作または画面状態変更で閉じる。

## 16. プレビュー設計

### 16.1 表示モード

```ts
type PreviewMode =
  | "questions"
  | "withAnswers"
  | "questionsAndAnswers";
```

`questionsAndAnswers`は`questions`セクションと`withAnswers`セクションを連続生成する。

### 16.2 RenderAtom

ページ分割単位は`RenderAtom`である。

```ts
type RenderAtom = {
  key: string;
  problem: ProblemBlock;
  number: string | null;
  content: ContentBlock | null;
  showSolution: boolean;
  showSolutionHeading: boolean;
  startsProblem: boolean;
  breakBefore: boolean;
  breakAfter: boolean;
};
```

問題内のトップレベルContentBlockを1 Atomずつ生成する。`pageBreak`自体は描画Atomにせず、直前Atomの`breakAfter`と次Atomの`breakBefore`へ変換する。

解答モードでは独立`answerArea`をAtom化しない。問題Solutionに可視内容があれば最後にSolution Atomを追加する。内容がない問題も空Atomを1件作る。

### 16.3 可視Solution判定

`hasVisibleDocument()`はtrim後の文字、数式、画像、表、スペーサーが1つでもあればtrueを返す。空段落だけのSolutionでは「解答」見出しを表示しない。

### 16.4 DOM計測

画面外の`.preview-measurement`へ各セクションを100%倍率で描画する。

1. `document.fonts.ready`を待つ。
2. 測定領域内の画像のload/errorを待つ。
3. `requestAnimationFrame`で測定する。
4. `ResizeObserver`でヘッダーとAtomの寸法変化を監視する。
5. ページ高さ、上下padding、ヘッダー高さ、問題間gap、各Atom外寸を取得する。

初回測定完了前は1セクション1ページのfallbackを表示する。測定完了後に`data-pagination-ready="true"`を設定する。

### 16.5 ページ割付

`paginateMeasuredItems()`は次の状態を持つ。

- 現在ページのAtom key配列
- 使用済み高さ
- 先頭ページ容量
- 2ページ目以降の容量

処理規則は次のとおり。

1. `breakBefore`かつ現在ページが非空ならページを確定する。
2. 新しい問題の開始Atomだけ、同ページ内で問題間gapを加算する。
3. 現在ページに入らない場合はページを確定し、次ページへ置く。
4. Atomを追加する。
5. `breakAfter`ならページを確定する。

先頭ページ容量はヘッダー高さを差し引き、2ページ目以降はヘッダーなしの容量を使用する。Atom単体が空ページ容量を超える場合でも内部分割や縮小を行わず、そのページへ配置する。

### 16.6 ページ描画

`.paper-page`は基準幅520pxへ倍率を掛け、用紙の縦横比を`aspect-ratio`で表現する。paddingは`marginMm / pageWidthMm * 100%`で設定する。ページ外は`overflow: hidden`で切り取る。

ヘッダーは各セクションの`sectionPageIndex === 0`だけに表示する。画面用のページカウンターは紙面外へ表示し、PDF用DOMでは非表示にする。

### 16.7 コンテンツ描画

- リッチテキスト：保存ASTをReact要素へ変換
- 囲み枠：presetクラスと任意題名
- 解答欄：線の反復または指定高さの枠
- スペーサー：`rows * 1.25em`
- 画像：配置classと幅%
- 表：列幅、rowSpan、colSpan、任意行高
- 小問：2列Grid、全幅は2列占有
- 数式：`MathFormula`

画像参照が見つからない場合は欠落メッセージを描画する。

### 16.8 倍率

数値倍率は25～200%を5%刻みで変更する。`fitWidth`はプレビュースクロール領域の横幅から左右paddingを除き、基準用紙幅520pxで割った倍率を使用する。`fitPage`はこの横幅倍率と、表示可能な高さから上下paddingおよびページカウンター領域を除いて算出した縦倍率の小さい方を使用する。倍率は25～200%へ制限し、`ResizeObserver`とウィンドウの`resize`でペイン実寸の変更へ追従する。算出した倍率は`.preview-pages`のCSS変数を介して全ページへ一括適用する。倍率表示中央のボタンにはクリック処理がない。

### 16.9 現行ページングの境界

次の内部分割は行わない。

- 長い段落やリストの行単位分割
- 囲み枠内部の分割
- 小問と解答欄の分割
- 表の縮小
- 縦長画像の自動縮小
- 1つの解答欄の行単位分割

そのため、1 Atomが1ページを超えると下部が切れる可能性がある。

## 17. PDF設計

### 17.1 出力導線

PDFダイアログで3モードの説明、用紙、ページ数、印刷注意を表示する。ダウンロード開始時に画面外のPDF用`WorksheetPreview`がページング完了するまで最大10秒待つ。

### 17.2 生成処理

`generateWorksheetPdf()`は次を行う。

1. 先頭ページが存在することを確認する。
2. `document.fonts.ready`とページ内画像の読込を待つ。
3. 2回の`requestAnimationFrame`でレイアウト確定を待つ。
4. `getFontEmbedCSS()`でプレビューに必要なWebフォントCSSを取得する。
5. 各`[data-preview-page="true"]`を`toPng()`でPNG化する。
6. React PDFで用紙寸法のPageを作り、PNGを全面へ配置する。
7. Blobを生成してダウンロードする。

PNG化設定は背景白、`pixelRatio: 4`、`skipAutoScale: true`である。用紙寸法はmmからptへ変換する。PDF Pageは`wrap={false}`とする。

PDF専用の組版は行わず、ブラウザが描画したプレビューを共有する。結果は画像PDFであり、文字選択・検索可能なテキストレイヤーは持たない。

### 17.3 ファイル名

`題名_問題のみ_YYYYMMDD-HHmm.pdf`など、題名、モード表示名、ローカル日時を連結する。ファイル名禁止文字を`_`へ置換し、連続`_`を圧縮し、末尾の空白とピリオドを除去して80文字へ切り詰める。

## 18. JSONバックアップ

### 18.1 ファイル構造

単一形式：

```ts
{
  format: "math-worksheet";
  kind: "single";
  version: 1;
  exportedAt: string;
  worksheet: Worksheet;
  assets: BackupAsset[];
}
```

全体形式：

```ts
{
  format: "math-worksheet";
  kind: "archive";
  version: 1;
  exportedAt: string;
  worksheets: Worksheet[];
  assets: BackupAsset[];
}
```

`BackupAsset`は`AssetRecord.blob`をdata URL接頭辞なしのBase64文字列`dataBase64`へ置換した構造である。

### 18.2 Asset選択

`collectReferencedAssetIds()`はWorksheetを再帰走査し、キー名が`assetId`で値が文字列のものを収集する。

- 単一：対象Worksheetが参照するAssetだけ
- 全体：`deletedAt === null`のWorksheetが参照するAssetだけ
- 未参照Assetを除外
- ゴミ箱専用Assetを除外

### 18.3 出力

Blobを`Uint8Array`へ変換し、32KiB単位でバイナリ文字列へ変換して`btoa`する。完成オブジェクトを`MathWorksheetFileSchema.parse`してから返す。

JSONは2スペースインデントでBlob化し、利用者が明示的にクリックできる`<a download>`用Object URLを準備する。キャンセル時は直ちに、ダウンロード開始時は60秒後にURLを破棄する。

ファイル名は次のとおり。

- 単一：`題名_YYYYMMDD-HHmm.json`
- 全体：`math-worksheet-backup-YYYYMMDD-HHmm.json`

### 18.4 バックアップ相関検証

ファイル全体で次を検証する。

- すべてのEntity IDとAsset IDが重複しない。
- Assetの`worksheetId`に対応するWorksheetがファイル内にある。
- すべての画像参照にAssetが存在する。
- Asset所有Worksheetと参照元Worksheetが一致する。
- 参照されない余剰Assetが存在しない。
- archiveにゴミ箱Worksheetを含めない。
- archiveは1～2,000件。

### 18.5 インポート

画面で100MiB以下を確認した後、ファイル本文を文字列として読み、`JSON.parse`とZod検証を行う。

`hydrateBackup()`は次を行う。

1. Worksheetごとに新しいWorksheet IDを生成する。
2. Worksheet配下のすべての`id`を再生成する。
3. 作成日時と更新日時を現在時刻へ設定する。
4. 各Assetへ新しいIDを生成する。
5. Worksheet内の`assetId`を新しいAsset IDへ置換する。
6. Base64を`Uint8Array`へ戻し、MIME型付きBlobを生成する。
7. `createMany()`で全件を単一トランザクション保存する。

既存データの削除やID単位のマージはしない。インポート時はBackupAssetのBase64形式、MIME列挙、正の寸法をSchema検証するが、画像バイトを`createImageBitmap`で再デコードせず、画像選択時の10MiB、10,000px、40MP上限を再確認しない。

## 19. エラー処理

| 場所 | 処理 |
|---|---|
| 一覧読込 | エラーパネルと再読込 |
| 不正Worksheet | 件数を警告し、データは保持 |
| 編集読込 | Not Found画面 |
| 自動保存 | `failed`表示と再試行 |
| 画像選択 | ダイアログ内メッセージ |
| JSON解析・保存 | インポートダイアログ内`role=alert` |
| PDF生成 | PDFダイアログ内エラー |
| 構造上限 | ボタン無効化またはToast |

例外を画面全体で捕捉するError Boundaryは実装していない。

## 20. セキュリティとデータ保全

### 20.1 入力制限

- Zod strict objectで未知フィールドを拒否する。
- リッチテキストを許可ASTへ正規化する。
- link、code、任意styleを保存しない。
- 危険なLaTeX外部参照コマンドを拒否する。
- SVG画像を許可しない。
- JSONの参照整合性をファイル全体で検証する。

MathLiveが生成した数式HTMLは`dangerouslySetInnerHTML`で描画する。入力LaTeXは保存Schemaの禁止コマンド検証対象であり、MathLiveの変換結果だけを挿入する。

### 20.2 データ保全

- WorksheetとAssetの同時作成・更新はIndexedDBトランザクションを使用する。
- ゴミ箱移動では即時に物理削除しない。
- JSONエクスポートを端末外バックアップ手段として提供する。
- 永続化前にWorksheetを再検証する。

サーバー送信処理はない。ブラウザデータ削除、別プロファイル、別オリジンでは保存データを利用できない。

## 21. アクセシビリティとキーボード

- 主要アイコンボタンへ`aria-label`またはtitleを設定する。
- Modalへdialog role、aria-modal、タイトル関連付けを設定する。
- ModalはEscで閉じ、Tabを循環し、閉じた後にfocusを戻す。
- Toastは`role=status`を使用する。
- 数式は`role=math`と読み上げ用`aria-label`を持つ。
- ペイン境界は`role=separator`、値域、現在値、左右矢印操作を持つ。
- 保存状態は`aria-live=polite`を使用する。

問題のドラッグハンドルと小問のGripアイコンは視覚表示だけであり、ドラッグ操作やキーボード並べ替えを実装しない。

## 22. スタイルとレスポンシブ設計

### 22.1 共通トークン

`styles.css`の`:root`へ色、フォント、角丸をCSS Custom Propertyとして定義する。主色は`#4a9fd8`、危険色は`#c62828`、背景は白と`#f6f8fa`を使用する。

### 22.2 画面幅

- 1280px以下：一覧とヘッダーの左右paddingを縮小し、プレビューの文字ボタンを非表示
- 1023px以下：編集ワークスペースを非表示、PC画面警告を表示
- 一覧は900pxの最小幅を持ち、狭い画面では横スクロールする。

### 22.3 フォント

用紙設定値に対応する`font-*`クラスは付与するが、現行CSSには書体別定義がない。プレビューは固定の`"BIZ UDPGothic", "Yu Gothic", sans-serif`を使用し、OSフォントへフォールバックする。選択した5種類の日本語フォントを切り替える処理と同梱Webフォントは未実装である。

## 23. Schema生成とビルド

### 23.1 Schema生成

`scripts/generate-math-worksheet-schema.ts`は`MathWorksheetFileSchema`からJSON Schemaを生成し、`schemas/math-worksheet.schema.json`へ出力する。

- `npm run schema:generate`：生成ファイルを更新
- `npm run schema:check`：現行ファイルとの差分を検査
- `npm run schema:test`：構造、上限、参照整合性、旧形式拒否を検査

### 23.2 ビルド

```text
npm run build
└─ tsc --noEmit
   └─ vite build
```

TypeScriptは`strict`、`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`を有効にする。ViteのブラウザターゲットはChrome 107、Edge 107である。

### 23.3 検証一式

```text
npm run verify
├─ npm run typecheck
├─ npm run schema:check
├─ npm run schema:test
├─ npm run manual:check
└─ npm run test

npm run test:e2e
└─ Playwright（離脱・保存の実ブラウザ検証）
```

## 24. テスト設計

### 24.1 テスト環境

- Vitest
- jsdom
- Testing Library
- `@testing-library/jest-dom`
- `fake-indexeddb`
- Playwright（ローカルはGoogle Chrome、CIはChromium）

`src/test/setup.ts`でブラウザAPIのテスト用設定を行う。Playwrightは`e2e/global-setup.ts`でViteを起動し、テスト終了時に同一プロセス内で停止する。

### 24.2 テストファイル対応

| テスト | 主な検証対象 |
|---|---|
| `worksheet.behavior.test.ts` | 既定値、検索、採番、Command、画像参照、用紙計算 |
| `table-operations.test.ts` | 行列、結合、分割、上限、行高、列幅 |
| `backup.test.ts` | 参照Assetだけの単一・全体出力 |
| `repository.test.ts` | CRUD、ゴミ箱、未参照Asset GC、JSON復元 |
| `download.test.ts` | 利用者がクリック可能なJSON URL |
| `generate-pdf.test.tsx` | PDF各ページへのフォントCSS共有 |
| `editor-store.test.ts` | 部分更新の構造共有、入力履歴結合、Undo / Redo、revision、編集セッションをまたぐ保存競合 |
| `editor-assets.test.ts` | Undo / Redo中のAsset保持とObject URL解放 |
| `EditorScreen.integration.test.tsx` | beforeunload、debounce保存、保存失敗・再試行、離脱前保存、再読込復元 |
| `pagination.test.ts` | オーバーフロー、継続、明示改ページ、ヘッダー差 |
| `RichTextEditor.test.tsx` | 数式、正規化、表セル、画像、ツールバー |
| `ProblemCard.test.tsx` | 追加メニューと本文系挿入ボタン |
| `EditorDialogs.test.tsx` | 既存画像の設定変更 |
| `WorksheetListScreen.test.tsx` | 空状態、検索、単一・全体JSON導線 |
| `e2e/editor-lifecycle.spec.ts` | Chromeでのタブ終了警告、リロード復元、ブラウザ戻る時の保存 |

現行のVitestは23ファイル、125件である。Playwright E2Eは1ファイル、3件である。実ブラウザでの印刷比較テストはない。

## 25. 既知の実装境界

1. 問題・小問のドラッグ＆ドロップは未接続である。
2. 小問複製、並べ替え、小問Solution編集は未実装である。
3. 段落揃えは保存・描画できるが、変更UIがない。
4. 問題Solutionへ数式、画像、表を挿入するUIがない。
5. 独立画像・独立表を「内容を追加」から新規作成できない。
6. フォント設定値は保存するが、選択書体をプレビューとPDFへ反映しない。
7. ページ分割はトップレベルAtom単位で、内部行分割・縮小をしない。
8. PDFはラスタ画像で、検索可能な文字を持たない。
9. インポート画像の再デコードと挿入時上限の再検証をしない。
10. 未保存時の離脱警告は行うが、タブ終了直前の保存完了保証と複数タブ競合制御はない。
11. `editLocks`は実機能へ接続されていない。
12. 未参照Assetの削除は通常の編集終了時に行うため、ブラウザプロセスの強制終了時は次回の編集終了まで残る場合がある。
13. Toastの自動消去はない。
14. アプリ全体を覆うError Boundaryはない。

## 26. 要件トレーサビリティ

| 要件領域 | 主な実装 |
|---|---|
| プリント管理 | `WorksheetListScreen.tsx`、`TrashScreen.tsx` |
| 編集・自動保存 | `EditorScreen.tsx`、`editor-store.ts` |
| 問題・小問 | `ProblemCard.tsx`、`worksheet.commands.ts` |
| リッチテキスト | `RichTextEditor.tsx`、`rich-text-editor-extensions.ts` |
| 数式 | `MathFormula.tsx`、`MathDialog` |
| 画像 | `ImageDialog`、`AssetRecord`、`putAsset` |
| 表 | `table-operations.ts`、`TableStructureToolbar.tsx` |
| プレビュー | `WorksheetPreview.tsx`、`pagination.ts` |
| PDF | `generate-pdf.tsx`、`PdfDialog` |
| 自動保存・履歴 | `EditorScreen.tsx`、`editor-store.ts` |
| JSON | `backup.ts`、`MathWorksheetFileSchema` |
| 永続化 | `database.ts`、`dexie-worksheet-repository.ts` |
| 検証 | `worksheet.schema.ts`、`scripts/`、各テスト |

## 27. 変更時の保守手順

### 27.1 データ構造を変更する場合

1. `worksheet.schema.ts`を変更する。
2. 必要なら`structure-limits.ts`を変更する。
3. `worksheet.ts`の公開型が自動導出されることを確認する。
4. defaults、commands、backup、preview、Repositoryを追従させる。
5. Schema振る舞いテストを追加する。
6. `npm run schema:generate`を実行する。
7. `npm run verify`、`npm run test:e2e`、`npm run build`を実行する。

既存データ移行が必要になる変更では、version 1のまま互換性を壊さず、新しいversionと移行処理を設計する。現行コードには移行基盤がないため、Schemaだけを先行変更しない。

### 27.2 UI機能を変更する場合

- Domain制約とUIのdisabled条件を一致させる。
- PreviewとPDFが同じDOMを共有する前提を保つ。
- Object URLの生成と破棄を対応させる。
- Undo / Redo対象と自動保存状態を確認する。
- Testing Libraryで操作導線を追加検証する。

## 28. 完了条件

本書と実装の整合確認は、次をすべて満たした時点で完了とする。

- `npm run verify`が成功する。
- `npm run test:e2e`が成功する。
- `npm run build`が成功する。
- 生成JSON SchemaがZod Schemaと一致する。
- 新規作成、編集、再読込、ゴミ箱、JSON、PDFの主要導線がコードと本書で一致する。
- 未実装事項を実装済みとして記載していない。
- `README.md`、`要件定義書.md`、本書の制約記述が矛盾しない。
