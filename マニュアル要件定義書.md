# 数学プリント作成ソフト マニュアル要件定義書 v1.0

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 数学プリント作成ソフト マニュアル要件定義書 |
| 文書版 | 1.0 |
| 基準日 | 2026-08-13 |
| 対象 | 数学プリント作成ソフトのアプリ内マニュアル |
| 提供形態 | PC向けWebアプリケーション内のヘルプ画面 |
| 関連文書 | `要件定義書.md`、`画面設計.md`、`詳細設計書.md` |
| 実装状態 | 未実装。本書は実装予定の要件を定義する |

本書は、数学プリント作成ソフトへ追加するアプリ内マニュアルと、各機能から該当する説明へ移動する文脈リンクの要件を定義する。マニュアル本文の詳細な文章、画面設計上の寸法、コンポーネント単位の内部設計は、実装時に本書を基に別途定義する。

## 2. 目的

### 2.1 導入目的

- 初めて利用する教員が、外部資料を参照せずに最初のプリントを作成できるようにする。
- 利用中に分からない操作が発生したとき、現在の作業を失わずに関連する説明を確認できるようにする。
- 数式、画像、表、プレビュー、PDF、バックアップなど、操作上の注意が多い機能を正しく利用できるようにする。
- ブラウザ内保存の性質とバックアップの必要性を明確に伝え、意図しないデータ消失を防ぐ。
- アプリとマニュアルを同一リポジトリで管理し、機能と説明の不一致を抑制する。

### 2.2 基本方針

- マニュアルはアプリ本体の一画面として提供する。
- マニュアル本文はMarkdownファイルを正本とする。
- 一覧、編集、各種ダイアログから関連する章へ直接移動できる文脈リンクを提供する。
- マニュアルはアプリと同じビルド成果物に含め、外部サーバー、CMS、アカウント、ネットワーク接続を必要としない。
- 編集作業を維持するため、アプリ本体からマニュアルを開く場合は原則として別タブで開く。
- 初期リリースでは単純で保守しやすい構成を優先し、専用ドキュメントサイトや高度な全文検索基盤は導入しない。

## 3. 対象利用者と利用環境

### 3.1 対象利用者

- 中学校数学のプリントを作成する教員
- PCの基本操作とブラウザ操作は行えるが、Markdown、HTML、JSONなどの技術知識を前提としない利用者

### 3.2 対象環境

- 最新版のGoogle ChromeまたはMicrosoft Edge
- 横幅1024px以上のPC画面
- JavaScriptが有効であること
- マニュアル閲覧自体はIndexedDBへデータを読み書きせず、アプリのプリントデータへ依存しないこと

### 3.3 PC専用表示

- マニュアルの閲覧もPC専用とする。
- 画面幅が1024px未満の場合は通常のマニュアル画面を表示せず、PCサイズの画面で開くよう案内する。
- スマートフォンおよびタブレット向けのレイアウト最適化、折りたたみ目次、タッチ専用操作は初期リリースの対象外とする。

## 4. 対象範囲

### 4.1 初期リリースに含むもの

- 章単位のアプリ内マニュアル
- マニュアル目次
- 章タイトル、キーワード、本文を対象とした検索
- 前の章、次の章への移動
- アプリ本体の主要画面および主要ダイアログからの文脈リンク
- 章ごとの固定URL
- Markdownの表、リスト、リンク、画像、注意書きに適した表示
- キーボード操作とスクリーンリーダーを考慮した基本的なアクセシビリティ
- マニュアルの版、対象アプリ版、最終更新日の表示
- リンク、目次、検索、Markdown表示に対する自動テスト

### 4.2 初期リリースに含まないもの

- 初回起動時のツアーまたはステップ型チュートリアル
- マニュアル専用PDFの生成および配布
- 動画、音声、アニメーション、操作シミュレーター
- 利用者によるコメント、評価、問い合わせ投稿
- クラウドCMSや外部ドキュメントサービスとの連携
- マニュアルの複数言語対応
- マニュアルの複数バージョン切り替え
- FlexSearchなどを利用した高度な全文検索、あいまい検索、検索候補
- MDXによる任意のReactコンポーネント埋め込み
- Docusaurusなどによる独立ドキュメントサイト
- 見出し単位の自動URL生成と共有リンク

## 5. 技術構成

### 5.1 採用構成

| 目的 | 採用方式 |
|---|---|
| UI基盤 | 既存のReact 19およびTypeScript |
| ルーティング | 既存のReact Router |
| ビルド | 既存のVite |
| Markdown読込み | Viteの`?raw`インポート |
| Markdown表示 | `react-markdown` |
| Markdown拡張 | `remark-gfm` |
| 検索 | アプリ内の単純な文字列部分一致 |
| アイコン | 既存の`lucide-react` |
| スタイル | 既存のデザイントークンとCSS |

### 5.2 依存ライブラリ

初期リリースで新規に追加するライブラリは、原則として次の2件に限定する。

```text
react-markdown
remark-gfm
```

- `react-markdown`でMarkdownをReact要素へ変換する。
- `remark-gfm`で表、取り消し線、タスクリスト、自動リンク、脚注などのGitHub Flavored Markdownを扱う。
- `rehype-raw`は導入せず、Markdown内の任意HTMLを実行または描画しない。
- `MDX`、`Docusaurus`、`FlexSearch`は初期リリースでは導入しない。

### 5.3 コンテンツ構成

実装時の配置は次を基本とする。実際のファイル分割は章立ての変更に応じて増減できるが、本文と表示処理を分離する。

```text
src/manual/
├─ manual-index.ts
├─ ManualScreen.tsx
├─ ManualMarkdown.tsx
├─ manual-search.ts
└─ content/
   ├─ overview.md
   ├─ getting-started.md
   ├─ worksheet-list.md
   ├─ editor-basics.md
   ├─ formulas.md
   ├─ images-and-tables.md
   ├─ answers.md
   ├─ preview-and-pdf.md
   ├─ saving-and-history.md
   ├─ backup-and-trash.md
   ├─ troubleshooting.md
   └─ version-and-license.md
```

- `content/`配下のMarkdownを説明本文の正本とする。
- `manual-index.ts`で章の順序、固定slug、題名、概要、検索用キーワード、対象アプリ版、最終更新日を管理する。
- Markdown内へ表示順やルーティングを決定する実行コードを記述しない。
- マニュアルで使用する画像はアプリのビルド成果物へ含め、外部URLへ依存しない。

## 6. ルーティング

### 6.1 ルート一覧

| パス | 画面 | 動作 |
|---|---|---|
| `/help` | マニュアル入口 | 先頭章またはマニュアル概要を表示する |
| `/help/:chapterSlug` | マニュアル章 | 指定した固定slugの章を表示する |

### 6.2 ルーティング要件

- 各章は一意で変更しにくい英小文字のslugを持つ。
- 文脈リンクは章タイトルではなくslugを参照する。
- 存在しない`chapterSlug`へアクセスした場合は、マニュアル内の「ページが見つかりません」を表示し、目次と先頭章への導線を提供する。
- `/help`配下の不正なURLをプリント一覧へ無条件に転送しない。
- 章を切り替えた場合は本文領域の先頭へスクロールし、章見出しへフォーカスを移動できるようにする。
- ブラウザの戻る、進む操作で閲覧履歴を移動できるようにする。
- 初期リリースの文脈リンクは章単位とし、見出し文から自動生成したアンカーを恒久リンクとして使用しない。

## 7. マニュアル画面

### 7.1 基本レイアウト

マニュアル画面は次の領域で構成する。

1. ヘッダー
2. 左側の章目次
3. 中央の本文
4. 本文下部の前章・次章ナビゲーション

- 画面幅1024px以上で、左側目次と本文を同時に表示する。
- 本文の1行を過度に長くせず、日本語の説明文を読みやすい幅に制限する。
- 既存アプリの色、書体、ボタン、フォーカス表示を踏襲し、別製品のような外観にしない。
- 編集画面と異なり、プレビューとの分割ペインやドラッグ可能な境界は設けない。

### 7.2 ヘッダー

ヘッダーには次を表示する。

- アプリ名またはブランド
- 「使い方」または「マニュアル」という画面名
- マニュアル検索欄
- アプリへ移動する導線

別タブで開いたマニュアルを閉じる操作はブラウザへ委ね、スクリプトによるタブ終了は行わない。

### 7.3 目次

- 章を定義順に表示する。
- 現在表示している章を視覚表示と`aria-current="page"`で識別する。
- 各章の題名をリンクとして操作できるようにする。
- 目次は本文をスクロールしても利用できる位置に維持する。ただし、画面高が不足する場合は目次領域自体をスクロール可能にする。
- 目次の順序と前章・次章の順序は、同一の章定義を正本として導出する。

### 7.4 本文

- 章題名、概要、本文を表示する。版情報とライセンスは専用章に表示する。
- 見出し階層を飛ばさず、章題名を`h1`、章内の大見出しを`h2`以下として構造化する。
- 表が本文幅を超える場合は、ページ全体ではなく表の表示領域を横スクロール可能にする。
- 画像は本文幅を超えて表示せず、代替テキストを必須とする。
- 画面写真には、操作説明に必要な箇所が分かる説明またはキャプションを付ける。
- 注意、重要、ヒントは色だけで区別せず、ラベルまたはアイコンと文言を併用する。

### 7.5 前章・次章

- 先頭章では「前の章」を表示しない、または無効化する。
- 最終章では「次の章」を表示しない、または無効化する。
- リンクには移動先の章題名を表示する。

## 8. Markdown表示要件

### 8.1 対応記法

- 見出し
- 段落、改行
- 順序付きリスト、順序なしリスト
- 太字、斜体、取り消し線
- 引用
- インラインコード、コードブロック
- リンク
- 画像
- 表
- 水平線
- タスクリスト
- 脚注

### 8.2 独自表示

`react-markdown`の要素差し替え機能を使用し、少なくとも次をアプリ向けに調整する。

- アプリ内リンク
- 外部リンク
- 画像とキャプション
- 表のスクロール領域
- コードブロック
- 見出し

Markdown内のHTMLは無視する。注意書きなどの専用表現が必要な場合は、任意HTMLやJSXを許可せず、安全なMarkdown記法をアプリ側で管理されたReactコンポーネントへ変換する方式を採る。

### 8.3 リンク

- `/help/`から始まるリンクはマニュアル内リンクとして扱う。
- アプリ画面へのリンクは既存のルート定義に従う。
- 外部リンクを使用する場合は`http`または`https`だけを許可し、別タブで開いて`rel="noopener noreferrer"`を付ける。
- `javascript:`、`data:`など、許可していないスキームをリンク先として実行しない。
- マニュアルの主要説明は外部リンクを開かなければ理解できない構成にしない。

### 8.4 画像

- マニュアル画像はローカルアセットだけを使用し、閲覧時に外部サーバーから取得しない。
- 画像ファイル名は用途が判別できる名前とし、章から安定して参照できるようにする。
- UI変更のたびに多数の画像更新が必要になる構成を避け、画面写真は操作位置の理解に効果がある箇所へ限定する。
- 画像が読み込めない場合も、代替テキストから目的を理解できるようにする。

## 9. 検索

### 9.1 検索対象

- 章題名
- 章概要
- 章ごとの検索用キーワード
- Markdown本文の表示テキスト

コード記法、Markdown記号、画像パスなど、利用者が画面上で説明として読まない文字列は可能な範囲で検索対象から除外する。

### 9.2 検索方法

- 初期リリースでは外部検索ライブラリを使用せず、文字列の部分一致で検索する。
- 検索語と検索対象へUnicode NFC正規化、全角ASCIIおよび全角空白の半角化、英字の小文字化、前後空白の除去を行う。
- 空白で区切られた複数語が入力された場合は、すべての語を含む章を一致とする。
- ひらがなとカタカナ、漢字の表記ゆれ、類義語は自動変換せず、必要な表記を章の検索用キーワードへ登録する。
- 入力中に検索を更新し、検索実行ボタンを必須としない。

### 9.3 検索結果

- 一致した章の題名、概要または一致箇所付近の短い抜粋を表示する。
- 結果を選択すると該当章へ移動する。
- 一致件数を表示する。
- 結果がない場合は、別の語句を試す案内と検索解除操作を表示する。
- 検索結果の順序は、題名一致、キーワード一致、本文一致を考慮しつつ、同条件では章の定義順とする。
- 検索語をプリントデータ、IndexedDB、LocalStorageまたは外部サービスへ保存、送信しない。

## 10. 文脈リンク

### 10.1 共通動作

- 文脈リンクは、利用者が操作している機能に対応するマニュアル章を直接開く。
- 原則として`target="_blank"`で別タブに開き、編集中のプリント、入力内容、開いているダイアログを維持する。
- リンクをアイコンだけで表示する場合は、ツールチップと読み上げ用の名前を付ける。
- リンク文言は「詳しい使い方」「バックアップについて」など、移動先の内容が推測できる表現にする。
- 文脈リンクを利用しなくても主要操作を完了できるようにし、ヘルプ閲覧を必須手順にしない。

### 10.2 最低限の配置

| 起点 | 表示形式 | 移動先 |
|---|---|---|
| プリント一覧ヘッダー | 「使い方」文字付きリンクまたはボタン | マニュアル入口または「はじめに」 |
| 編集画面ヘッダー | ヘルプアイコン | 「編集の基本」 |
| 設定・バックアップダイアログ | テキストリンク | 「バックアップと復元」 |
| JSONインポートダイアログ | テキストリンク | 「バックアップと復元」 |
| プリント設定ダイアログ | テキストリンク | 「プリント設定とプレビュー」 |
| PDF出力ダイアログ | テキストリンク | 「プレビューとPDF出力」 |
| 数式入力・編集ダイアログ | テキストリンク | 「数式」 |
| 画像入力・編集ダイアログ | テキストリンク | 「画像と表」 |
| 表入力ダイアログまたは表操作部 | テキストリンク | 「画像と表」 |
| ゴミ箱画面 | テキストリンク | 「バックアップとゴミ箱」 |

### 10.3 対応管理

- 文脈リンクの起点と章slugの対応は、画面コンポーネントへ文字列として散在させず、型付けされた定義から参照できる構成を基本とする。
- 削除または変更した章slugを参照する文脈リンクがないことを、自動テストまたはビルド時検証で確認する。
- 章を統合または改名する場合も、既存slugは可能な限り維持する。

## 11. 章立てと必須内容

初期リリースでは、少なくとも次の内容を提供する。内容量に応じて章を分割または統合できるが、文脈リンクの移動先は維持する。

初期の固定slugは次を基本とする。

| 順序 | 章 | 固定slug |
|---:|---|---|
| 1 | はじめに・動作環境 | `overview` |
| 2 | 最初のプリントを作る | `getting-started` |
| 3 | プリント一覧 | `worksheet-list` |
| 4 | 編集の基本 | `editor-basics` |
| 5 | 数式 | `formulas` |
| 6 | 画像と表 | `images-and-tables` |
| 7 | 教師用の正解・解説 | `answers` |
| 8 | プリント設定・プレビュー・PDF | `preview-and-pdf` |
| 9 | 保存と操作履歴 | `saving-and-history` |
| 10 | バックアップとゴミ箱 | `backup-and-trash` |
| 11 | トラブル対応・制約・ショートカット | `troubleshooting` |
| 12 | バージョンとライセンス | `version-and-license` |

章の統合、分割または題名変更を行っても、公開済みの固定slugは互換性のため可能な限り維持する。

### 11.1 はじめに・動作環境

- アプリの目的
- 対応ブラウザとPC画面幅
- アカウント、クラウド保存、同期、共同編集がないこと
- データが使用中のブラウザ内に保存されること
- ブラウザデータの削除や別PCへの移動に備えて、JSONバックアップが必要であること

### 11.2 最初のプリントを作る

- 新しいプリントの作成
- 題名と問題文の入力
- プレビュー確認
- PDF出力
- 最初のバックアップ

この章だけで、初めての利用者が単純な1問のプリントを作成し、PDFを保存できる内容とする。

### 11.3 プリント一覧

- 一覧、検索、新規作成、複製
- 単一プリントのJSONエクスポート
- インポート
- ゴミ箱への移動

### 11.4 編集の基本

- 編集画面の左右ペイン
- 題名、自動保存状態、Undo / Redo
- 問題の追加、複製、削除、移動、採番
- 本文、囲み枠、小問、解答欄、スペーサー、改ページ
- 教師用の正解・解説への導線

### 11.5 数式

- 行内数式と独立数式の違い
- 数式パレットと数式フィールド
- 既存数式の編集
- 表セル内数式
- 入力制限と利用できないコマンド

### 11.6 画像と表

- 対応画像形式、容量、寸法制限
- 画像の挿入、差し替え、幅、配置、代替テキスト
- 表テンプレート
- 行列追加、削除、結合、分割、行高、列幅
- 表および画像がページに収まらない場合の注意

### 11.7 教師用の正解・解説

- 問題のみ、解答付き、問題＋解答の違い
- 正解・解説の入力
- 生徒用解答欄との表示関係
- 現行UIで編集できない小問別正解などの制約

### 11.8 プリント設定・プレビュー・PDF

- 用紙、余白、ヘッダー、フォント設定
- プレビューモード
- 拡大、縮小、幅に合わせる、ページ全体
- 自動ページ分割と明示改ページ
- PDF出力手順
- PDFがラスタ画像であり、文字検索や選択ができないこと

### 11.9 保存と操作履歴

- 自動保存のタイミングと状態表示
- 保存失敗時の対応
- Undo / Redoとショートカット
- 再読み込み後にUndo履歴が残らないこと
- タブを閉じる直前の変更に関する注意

### 11.10 バックアップとゴミ箱

- 単一バックアップと全体バックアップの違い
- ゴミ箱内のプリントが全体バックアップへ含まれないこと
- JSONインポートが既存データを置換せず追加すること
- 復元、完全削除、ゴミ箱を空にする操作
- 完全削除が取り消せないこと
- 定期的なバックアップの推奨

### 11.11 トラブル対応・制約・ショートカット

- アプリを開けない場合
- プリントまたは画像を読み込めない場合
- 保存できない場合
- PDF出力が完了しない場合
- ページ下部が切れる場合
- 利用上限と既知の制約
- 対応しているキーボードショートカット
- 解決しない場合に確認すべき情報

### 11.12 バージョンとライセンス

- アプリ版、マニュアル版、ライセンス版、最終更新日
- 著作権表示
- 正式なライセンス文書の確認先
- 商用・組織利用、再配布などの問い合わせ先

## 12. コンテンツ作成要件

### 12.1 文体

- 利用者へ直接語りかける平易な日本語を使用する。
- 技術用語は必要最小限とし、使用する場合は最初に説明する。
- ボタン名や画面名は実際のUI表示と一致させ、かぎ括弧で示す。
- 1つの手順に複数の目的を詰め込まず、操作順を番号付きリストで示す。
- 制約と障害を区別し、正常な仕様をエラーのように記述しない。

### 12.2 注意事項

次の内容は、関連する章だけでなく「はじめに」または「最初のプリントを作る」でも目立つように説明する。

- データの保存先は現在使用しているブラウザ内である。
- アカウントおよびクラウド保存はない。
- 別PCまたは別ブラウザへデータは自動移動しない。
- ブラウザデータを削除すると保存済みプリントを失う可能性がある。
- 定期的にJSONバックアップを作成する。
- PDF出力前にプレビューでページ切れを確認する。

### 12.3 UIとの同期

- マニュアルに記載する機能、上限値、対応形式は、現行コードおよび`要件定義書.md`を根拠とする。
- 未実装の機能を利用可能であるかのように記述しない。
- UI文言を変更した場合は、同じ変更でマニュアル内の該当文言を確認する。
- 機能を追加、削除、変更するプルリクエストまたは変更作業では、マニュアル更新要否を確認項目に含める。

### 12.4 版情報

- 目次の最終章「バージョンとライセンス」に、マニュアル版、対象アプリ版、ライセンス版、著作権表示、最終更新日を表示する。
- 章ごとに最終更新日を持たせることは任意とする。
- アプリの機能とマニュアルの内容が一致しない可能性がある場合は、対象版を確認できるようにする。

## 13. セキュリティとプライバシー

- マニュアル本文はビルド時に同梱されたMarkdownだけを対象とし、利用者が任意のMarkdownを読み込ませる機能は提供しない。
- `dangerouslySetInnerHTML`および`rehype-raw`を使用してMarkdown内HTMLを描画しない。
- Markdown表示では`skipHtml`相当の設定を有効にする。
- URLの許可方式を独自に緩和せず、危険なプロトコルを拒否する。
- マニュアルの閲覧履歴、検索語、クリック履歴を外部へ送信しない。
- マニュアル閲覧のためにプリント本文、画像、バックアップなどの利用者データを読み取らない。
- 外部のフォント、画像、スクリプト、解析サービスをマニュアル表示の必須要素としない。

## 14. アクセシビリティ

- すべてのリンク、検索欄、目次、前章・次章をキーボードだけで操作できるようにする。
- フォーカス位置を視認できるようにする。
- ページ内に1つの`h1`を置き、見出し階層を保つ。
- 目次は`nav`要素と識別可能な名前を持つ。
- 検索結果件数の更新をスクリーンリーダーへ通知できるようにする。
- アイコンだけの文脈リンクには`aria-label`を付ける。
- 現在の章を色だけで示さない。
- 画像へ内容に応じた代替テキストを付け、装飾画像は空の代替テキストとする。
- 表には見出しセルを設け、必要に応じて説明文を付ける。
- `prefers-reduced-motion`を尊重し、閲覧に不要なアニメーションを必須としない。

## 15. 性能とオフライン動作

- マニュアル表示のためのネットワーク通信を発生させない。
- アプリ本体を配信済みの状態では、ネットワーク接続がなくてもマニュアル本文、目次、ローカル画像を表示できるようにする。ただし、アプリ全体を初回取得する仕組みやPWAキャッシュは本要件の対象外とする。
- 章の切り替えと検索入力に対して、通常量のマニュアルで操作を妨げる長時間の同期処理を行わない。
- 初期リリースでは全章を対象に単純検索できる規模を上限とし、表示上の遅延が確認された場合に検索インデックス導入を再検討する。

## 16. エラーおよび空状態

- Markdown本文を取得または表示できない場合は、空白画面にせず、章を表示できない旨と目次へ戻る操作を表示する。
- 不明な章slugではマニュアル内のNot Found表示を行う。
- 検索語が空の場合は通常の目次または検索前の状態を表示する。
- 検索結果が0件の場合は検索語を表示し、検索解除操作を提供する。
- 画像読込み失敗時も本文の閲覧を継続できるようにする。

## 17. テスト要件

### 17.1 単体テスト

- 検索文字列の正規化
- 単一語および複数語検索
- 題名、キーワード、本文の検索
- 検索結果の安定した並び順
- 章slugの一意性
- 前章、次章の算出
- 文脈リンクが存在する章slugだけを参照すること

### 17.2 UIテスト

- `/help`でマニュアル入口を表示すること
- `/help/:chapterSlug`で指定章を表示すること
- 不明な章slugでマニュアル内Not Foundを表示すること
- 目次から章を切り替えられること
- 現在章に`aria-current="page"`が付くこと
- 検索結果から章へ移動できること
- 検索結果0件の表示と解除操作
- 前章、次章の境界動作
- Markdownの表、リンク、画像、リストを表示できること
- Markdown内のHTMLが実行または描画されないこと
- 外部リンクに安全な属性が付くこと
- 1024px未満でPC利用案内を表示すること

### 17.3 結合確認

- プリント一覧の「使い方」からマニュアルを別タブで開けること
- 編集画面のヘルプから「編集の基本」を別タブで開けること
- 各主要ダイアログの文脈リンクが対応章を開くこと
- マニュアルを開いても編集中のプリントと未確定の画面状態が維持されること
- マニュアル閲覧がIndexedDBのプリントデータを変更しないこと
- 通常ビルドへすべてのMarkdown本文とローカル画像が含まれること

### 17.4 文書検証

- 章定義に対応するMarkdownファイルがすべて存在すること
- 孤立したMarkdownファイルがないこと
- マニュアル内リンクの参照先が存在すること
- すべての画像参照先が存在すること
- すべての意味を持つ画像に代替テキストがあること
- UIで利用する文脈リンクの対応先が存在すること

## 18. 受け入れ条件

初期リリースは、次をすべて満たした場合に受け入れ可能とする。

1. 最新版のChromeおよびEdge、横幅1024px以上でマニュアルを閲覧できる。
2. `/help`と各章の固定URLへ直接アクセスできる。
3. 左側目次、本文、前章・次章を使用して全章を移動できる。
4. 章題名、キーワード、本文を日本語の部分一致で検索できる。
5. 一覧、編集、バックアップ、インポート、設定、PDF、数式、画像、表、ゴミ箱から関連章へ移動できる。
6. 文脈リンクでマニュアルを開いた後も、元の編集作業を継続できる。
7. Markdownの主要記法とGFMの表を既存デザインに合わせて表示できる。
8. Markdown内の任意HTMLおよび危険なURLを実行しない。
9. マニュアルの表示と検索が外部通信およびプリントデータの読取りを必要としない。
10. 「ブラウザ内保存」「クラウド保存なし」「JSONバックアップ推奨」を利用者が見落としにくい位置へ表示する。
11. 「最初のプリントを作る」だけを読んだ利用者が、単純なプリントの新規作成、問題入力、プレビュー確認、PDF保存を完了できる。
12. 定義した単体テスト、UIテスト、結合確認、文書検証が成功する。

## 19. 将来拡張の判断条件

### 19.1 検索ライブラリ

章数または本文量の増加により単純部分一致の応答性、検索精度、結果抜粋が不十分になった場合は、CJKを扱えるFlexSearchなどの導入を再検討する。導入時も検索はブラウザ内で完結させる。

### 19.2 MDX

静的な説明では理解しにくく、マニュアル内へ実際に操作できるUI例やインタラクティブなデモを埋め込む必要が生じた場合だけMDXを再検討する。単なる注意書きや画像表示を理由にMDXを導入しない。

### 19.3 PDF版

印刷配布、研修資料、ネットワークやアプリを開けない環境での閲覧要求が具体化した場合は、Markdown正本から印刷用またはPDF用成果物を生成する方式を検討する。アプリ版とPDF版の本文を別々に管理しない。

### 19.4 初回ガイド

マニュアルを用意しても初回利用者が基本操作を開始できないことが利用観察や問い合わせから確認された場合は、5ステップ程度の初回ガイドを検討する。初回ガイドはマニュアルの代替ではなく、対応章へ移動できる補助導線とする。

## 20. 既知の制約

- 初期リリースの検索は意味検索や表記ゆれの自動吸収を行わない。
- 見出し単位の恒久リンクは提供せず、文脈リンクは章先頭へ移動する。
- 画面写真はUI変更時に手動更新が必要である。
- マニュアルはPC専用であり、1024px未満の画面では通常閲覧できない。
- アプリ全体のPWA化またはオフラインキャッシュを保証するものではない。
- マニュアル単体のPDF出力と印刷レイアウトは初期リリースの対象外である。
