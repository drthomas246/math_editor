interface:
  display_name: "Math Editor 教科書取込"
  short_description: "教科書PDFの指定範囲を確認付きMath Editor JSONへ変換"
  default_prompt: "Use $math-editor-textbook-import to convert a textbook PDF range into a reviewed Math Editor JSON file."
policy:
  allow_implicit_invocation: true
